# Phase 5 — Transport Wi-Fi — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implémenter le transport Wi-Fi entre la webapp Mac (Python/FastAPI) et l'ESP32, avec abstraction `Transport`, fallback gracieux, panneau Statut webapp et tests devkit USB + Wi-Fi.

**Architecture:** Une nouvelle interface `Transport` (bas niveau, write_line/read_line) avec 3 implémentations (`SerialTransport`, `WiFiTransport`, `NullTransport`). Une couche haute `PlateauBridge` (remplace `uart_bridge.UartBridge`) qui encapsule heartbeat thread, lock TX, switch_transport, et les méthodes métier `forward_move`/`send_home`. Côté firmware, extension du sketch `bringup_l298n_complet.cpp` avec `WiFi.softAP` + `WiFiServer` port 3333 + politique "dernier client gagne" + watchdog 30s.

**Tech Stack:** Python 3.12, FastAPI, pyserial (stdlib `socket`/`threading`), Arduino C++ (framework ESP32 Arduino, `WiFi.h` intégré), pytest avec markers `devkit_serial` / `devkit_wifi`.

**Spec:** `docs/superpowers/specs/2026-05-20-phase5-wifi-design.md`

**Adaptation au spec :** le spec écrit `asyncio.Lock` et `asyncio task` mais le `QuoridorService` actuel est en threading. Le plan adapte : `threading.Lock` + daemon thread. Pattern identique, cohérent avec l'existant (`_tick_thread` déjà en place).

**Adaptation au spec :** le port macOS détecté est `/dev/cu.usbserial-*` (pas `/dev/tty.usbserial-*`). Le `uart_bridge.py` existant utilise `cu.usbserial`. Le plan respecte ça.

---

## Vue d'ensemble des phases

| Phase | But | Hardware requis |
|---|---|---|
| **A** — Reconnaissance | Lire firmware, planifier les ajustements | Aucun |
| **B** — Abstraction Transport Python | Interface + 3 impls + factory, TDD complet | Aucun |
| **C** — Couche haute PlateauBridge | Heartbeat, lock, reconnexion, switch | Aucun |
| **D** — API + frontend Statut | Endpoints + panneau Statut + bannière | Aucun |
| **E** — Firmware ESP32 | softAP + WiFiServer + Stream* refactor | ESP32 USB |
| **F** — Outil wifi_switch + tests devkit | Helper macOS + tests USB & Wi-Fi | ESP32 USB |
| **G** — Validation finale | Run complet + démo locale Safari | ESP32 USB + Wi-Fi |

---

## File Structure

### Fichiers à créer
- `webapp/transport.py` — interface `Transport` + `SerialTransport` + `WiFiTransport` + `NullTransport` + `make_transport()` + `TransportError`
- `webapp/plateau.py` — couche haute `PlateauBridge` : heartbeat thread, `_tx_lock`, `forward_move`, `send_home`, `switch_transport`, métriques pour Statut
- `tests/webapp/test_transport_null.py`
- `tests/webapp/test_transport_serial.py`
- `tests/webapp/test_transport_wifi.py`
- `tests/webapp/test_transport_factory.py`
- `tests/webapp/test_plateau_bridge.py`
- `tests/webapp/test_api_status.py`
- `tests/webapp/test_api_transport_switch.py`
- `tests/devkit/__init__.py`
- `tests/devkit/conftest.py` (fixture `wifi_fixture`)
- `tests/devkit/test_handshake_serial.py`
- `tests/devkit/test_handshake_wifi.py`
- `tests/devkit/test_last_client_wins.py`
- `tests/devkit/test_serial_and_wifi_coexist.py`
- `tests/devkit/test_wall_parser.py`
- `tools/__init__.py`
- `tools/wifi_switch.py` (CLI macOS networksetup)

### Fichiers à modifier
- `firmware/src/bringup_l298n_complet.cpp` — ajout Wi-Fi softAP + WiFiServer + handle_command Stream*
- `webapp/server.py` — remplace l'init `uart_bridge` par `plateau.init()`, ajoute routes `/api/status` et `/api/transport/switch`
- `webapp/service.py` — remplace `_uart_bridge` par `_plateau`, met à jour `_forward_to_plateau_unlocked`, `new_game`, et `_to_dict_unlocked`
- `webapp/schemas.py` — ajoute `StatusResponse`, `TransportSwitchRequest`, `TransportSwitchResponse`
- `webapp/static/index.html` — ajoute popup panneau Statut + bannière dégradée
- `webapp/static/app.js` — polling `/api/status`, gestion panneau + bannière + boutons
- `webapp/static/style.css` — styles panneau + bannière + indicateurs colorés
- `pyproject.toml` — ajoute markers `devkit_serial` et `devkit_wifi`

### Fichiers à supprimer
- `webapp/uart_bridge.py` (migration vers `transport.py` + `plateau.py`)
- `tests/webapp/test_uart_bridge.py` (remplacé par tests transport + plateau)

---

## Phase A — Reconnaissance

### Task A1 : Lecture du sketch firmware pour valider l'absence de blocage

**But :** vérifier si `bringup_l298n_complet.cpp` contient des `delay()` longs qui empêcheraient `WiFiServer.accept()` de tourner. Si oui, ajuster le plan (introduire des yields). Si non, continuer sans modification structurelle.

**Files:**
- Read: `firmware/src/bringup_l298n_complet.cpp`

- [ ] **Step 1: Lire le fichier**

Run: `wc -l firmware/src/bringup_l298n_complet.cpp` puis `Read` du fichier.

- [ ] **Step 2: Rechercher les `delay()`**

Run: `grep -nE '\bdelay\(' firmware/src/bringup_l298n_complet.cpp`

Décision :
- Si tous les `delay()` font < 50 ms → **OK, plan inchangé**
- Si des `delay()` font 100 ms ou plus dans la `loop()` → **noter dans `docs/superpowers/plans/2026-05-20-phase5-wifi.md` qu'il faudra introduire un `yield()` ou découper en `millis()` non bloquant**

- [ ] **Step 3: Vérifier la structure de la `loop()`**

Confirmer : la `loop()` lit déjà `Serial.readStringUntil('\n')` et dispatche dans une fonction. Si oui, on peut refactorer en `handle_command(cmd, Stream*)`. Si non, identifier la structure pour adapter le plan.

- [ ] **Step 4: Pas de commit** — cette tâche est de la lecture pure, rien à committer.

---

## Phase B — Abstraction Transport Python

### Task B1 : Squelette `transport.py` avec interface abstraite

**Files:**
- Create: `webapp/transport.py`
- Create: `tests/webapp/test_transport_abstract.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_transport_abstract.py`:

```python
"""Tests de l'interface abstraite Transport."""
from __future__ import annotations

import pytest

from webapp.transport import Transport, TransportError


def test_transport_is_abstract():
    """Transport ne peut pas être instancié directement."""
    with pytest.raises(TypeError):
        Transport()  # type: ignore[abstract]


def test_transport_error_is_exception():
    """TransportError est une Exception standard."""
    err = TransportError("test")
    assert isinstance(err, Exception)
    assert str(err) == "test"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_transport_abstract.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'webapp.transport'`

- [ ] **Step 3: Write minimal implementation**

Create file `webapp/transport.py`:

```python
"""Abstraction Transport : canal bidirectionnel ligne par ligne vers l'ESP32.

Deux implémentations : SerialTransport (USB-série) et WiFiTransport (TCP).
Plus NullTransport pour le mode autonome explicite.

Tous les transports parlent le même protocole texte ligne par ligne (UTF-8),
ce qui permet à la couche haute (PlateauBridge) de rester agnostique.
"""
from __future__ import annotations

from abc import ABC, abstractmethod


class TransportError(Exception):
    """Erreur de transport (connexion impossible, coupure, encodage, etc.)."""


class Transport(ABC):
    """Canal bidirectionnel ligne par ligne vers l'ESP32."""

    @abstractmethod
    def open(self) -> None:
        """Établit la connexion. Lève TransportError si échec."""

    @abstractmethod
    def write_line(self, line: str) -> None:
        """Envoie une ligne (le \\n est ajouté automatiquement, UTF-8).

        Lève TransportError si l'écriture échoue.
        """

    @abstractmethod
    def read_line(self, timeout: float = 1.0) -> str | None:
        """Lit une ligne (sans \\n). Retourne None si timeout."""

    @abstractmethod
    def close(self) -> None:
        """Ferme proprement le canal. Idempotent."""

    @property
    @abstractmethod
    def is_alive(self) -> bool:
        """True si le canal est ouvert et présumé fonctionnel."""

    @property
    @abstractmethod
    def description(self) -> str:
        """Description lisible pour le panneau Statut.

        Ex: 'wifi 192.168.4.1:3333' ou 'serial /dev/cu.usbserial-110'.
        """
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_transport_abstract.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/transport.py tests/webapp/test_transport_abstract.py
git commit -m "feat(transport): interface abstraite Transport + TransportError

Squelette de la nouvelle couche transport Wi-Fi/USB. Pour l'instant,
seule l'interface abstraite est definie. Les 3 implementations (Serial,
WiFi, Null) suivent.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B2 : `NullTransport` — mode autonome explicite

**Files:**
- Modify: `webapp/transport.py` (ajout de la classe `NullTransport`)
- Create: `tests/webapp/test_transport_null.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_transport_null.py`:

```python
"""Tests de NullTransport (mode autonome explicite)."""
from __future__ import annotations

import pytest

from webapp.transport import NullTransport, TransportError


def test_null_transport_open_is_noop():
    t = NullTransport()
    t.open()  # ne doit pas lever


def test_null_transport_write_raises():
    t = NullTransport()
    t.open()
    with pytest.raises(TransportError):
        t.write_line("PING")


def test_null_transport_read_returns_none():
    t = NullTransport()
    t.open()
    assert t.read_line() is None


def test_null_transport_close_is_noop():
    t = NullTransport()
    t.open()
    t.close()
    t.close()  # idempotent


def test_null_transport_is_alive_false():
    t = NullTransport()
    assert t.is_alive is False


def test_null_transport_description():
    t = NullTransport()
    assert t.description == "none (mode autonome)"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_transport_null.py -v`
Expected: FAIL with `ImportError: cannot import name 'NullTransport'`

- [ ] **Step 3: Write minimal implementation**

Append to `webapp/transport.py`:

```python
class NullTransport(Transport):
    """Transport qui ne fait rien. Mode autonome explicite.

    Sert à éviter les Optional[Transport] partout dans le service.
    Toute tentative d'écriture lève TransportError.
    """

    def open(self) -> None:
        pass

    def write_line(self, line: str) -> None:
        raise TransportError("NullTransport : écriture impossible en mode autonome")

    def read_line(self, timeout: float = 1.0) -> str | None:
        return None

    def close(self) -> None:
        pass

    @property
    def is_alive(self) -> bool:
        return False

    @property
    def description(self) -> str:
        return "none (mode autonome)"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_transport_null.py -v`
Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/transport.py tests/webapp/test_transport_null.py
git commit -m "feat(transport): NullTransport pour mode autonome explicite

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B3 : `SerialTransport` — migration de `uart_bridge` vers la nouvelle interface

**Files:**
- Modify: `webapp/transport.py` (ajout de `SerialTransport`)
- Create: `tests/webapp/test_transport_serial.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_transport_serial.py`:

```python
"""Tests de SerialTransport avec MockSerial."""
from __future__ import annotations

from unittest.mock import patch

import pytest

from webapp.transport import SerialTransport, TransportError


def test_serial_transport_description_before_open():
    t = SerialTransport(port="/dev/cu.usbserial-110")
    assert "serial" in t.description.lower()
    assert "/dev/cu.usbserial-110" in t.description


def test_serial_transport_auto_detect_port(monkeypatch):
    """SerialTransport sans port explicite cherche le premier /dev/cu.usbserial-*."""
    fake_ports = ["/dev/cu.usbserial-110", "/dev/cu.usbserial-220"]
    monkeypatch.setattr("webapp.transport._find_serial_ports", lambda: fake_ports)
    t = SerialTransport()
    assert t._port == "/dev/cu.usbserial-110"


def test_serial_transport_auto_detect_no_port(monkeypatch):
    """SerialTransport sans port et sans /dev/cu.usbserial-* lève TransportError à l'open."""
    monkeypatch.setattr("webapp.transport._find_serial_ports", lambda: [])
    t = SerialTransport()
    with pytest.raises(TransportError, match="aucun port"):
        t.open()


def test_serial_transport_open_and_write(mock_serial, monkeypatch):
    """SerialTransport.write_line envoie 'line\\n' encodé UTF-8."""
    monkeypatch.setattr("webapp.transport._open_serial", lambda port, baud: mock_serial)
    t = SerialTransport(port="/dev/cu.usbserial-110")
    t.open()
    t.write_line("PING")
    assert mock_serial.get_tx() == b"PING\n"


def test_serial_transport_read_line(mock_serial, monkeypatch):
    """SerialTransport.read_line lit jusqu'à \\n et retourne sans \\n."""
    monkeypatch.setattr("webapp.transport._open_serial", lambda port, baud: mock_serial)
    t = SerialTransport(port="/dev/cu.usbserial-110")
    t.open()
    mock_serial.inject_rx(b"PONG\n")
    assert t.read_line(timeout=1.0) == "PONG"


def test_serial_transport_read_line_timeout(mock_serial, monkeypatch):
    """read_line retourne None si rien n'arrive."""
    monkeypatch.setattr("webapp.transport._open_serial", lambda port, baud: mock_serial)
    t = SerialTransport(port="/dev/cu.usbserial-110")
    t.open()
    # rien injecté
    assert t.read_line(timeout=0.01) is None


def test_serial_transport_is_alive(mock_serial, monkeypatch):
    monkeypatch.setattr("webapp.transport._open_serial", lambda port, baud: mock_serial)
    t = SerialTransport(port="/dev/cu.usbserial-110")
    assert t.is_alive is False
    t.open()
    assert t.is_alive is True
    t.close()
    assert t.is_alive is False


def test_serial_transport_close_idempotent(mock_serial, monkeypatch):
    monkeypatch.setattr("webapp.transport._open_serial", lambda port, baud: mock_serial)
    t = SerialTransport(port="/dev/cu.usbserial-110")
    t.open()
    t.close()
    t.close()  # ne doit pas lever


def test_serial_transport_open_failure(monkeypatch):
    """Si l'ouverture du port lève, on convertit en TransportError."""
    def raise_oserror(port, baud):
        raise OSError("port introuvable")
    monkeypatch.setattr("webapp.transport._open_serial", raise_oserror)
    t = SerialTransport(port="/dev/cu.usbserial-110")
    with pytest.raises(TransportError):
        t.open()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_transport_serial.py -v`
Expected: FAIL with `ImportError: cannot import name 'SerialTransport'`

- [ ] **Step 3: Write minimal implementation**

Append to `webapp/transport.py`:

```python
import glob
import logging
import platform
import time

log = logging.getLogger(__name__)


def _find_serial_ports() -> list[str]:
    """Cherche les ports série DevKit ESP32.

    Mac : /dev/cu.usbserial-*
    Linux : /dev/ttyUSB*, /dev/ttyAMA*
    """
    if platform.system() == "Darwin":
        return sorted(glob.glob("/dev/cu.usbserial-*"))
    ports = sorted(glob.glob("/dev/ttyUSB*"))
    if not ports:
        ports = sorted(glob.glob("/dev/ttyAMA*"))
    return ports


def _open_serial(port: str, baud: int):
    """Wrapper isolable (pour faciliter le mocking en tests)."""
    import serial
    return serial.Serial(port, baud, timeout=0.1, write_timeout=1.0)


class SerialTransport(Transport):
    """Transport USB-série via pyserial.

    Détection auto du port si non précisé (premier /dev/cu.usbserial-* sur Mac).
    """

    def __init__(self, port: str | None = None, baud: int = 115200):
        if port is None:
            ports = _find_serial_ports()
            self._port = ports[0] if ports else None
        else:
            self._port = port
        self._baud = baud
        self._serial = None
        self._rx_buffer = bytearray()

    def open(self) -> None:
        if self._port is None:
            raise TransportError("aucun port série DevKit ESP32 détecté")
        try:
            self._serial = _open_serial(self._port, self._baud)
        except Exception as e:
            raise TransportError(f"ouverture {self._port} échouée : {e}") from e

    def write_line(self, line: str) -> None:
        if self._serial is None:
            raise TransportError("SerialTransport non ouvert")
        try:
            self._serial.write((line + "\n").encode("utf-8"))
            self._serial.flush()
        except Exception as e:
            raise TransportError(f"écriture série échouée : {e}") from e

    def read_line(self, timeout: float = 1.0) -> str | None:
        if self._serial is None:
            return None
        deadline = time.monotonic() + timeout
        while time.monotonic() < deadline:
            # Bloque jusqu'au prochain octet ou timeout pyserial (0.1s)
            chunk = self._serial.read(self._serial.in_waiting or 1)
            if chunk:
                self._rx_buffer.extend(chunk)
                if b"\n" in self._rx_buffer:
                    line, _, rest = self._rx_buffer.partition(b"\n")
                    self._rx_buffer = bytearray(rest)
                    return line.decode("utf-8", errors="replace").rstrip("\r")
        return None

    def close(self) -> None:
        if self._serial is not None:
            try:
                self._serial.close()
            except Exception:
                pass
            self._serial = None

    @property
    def is_alive(self) -> bool:
        return self._serial is not None

    @property
    def description(self) -> str:
        return f"serial {self._port or 'aucun port détecté'}"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_transport_serial.py -v`
Expected: PASS (9 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/transport.py tests/webapp/test_transport_serial.py
git commit -m "feat(transport): SerialTransport (migration de uart_bridge)

Refactor de la couche serie en SerialTransport conforme a l'interface
Transport. Comportement applicatif identique a uart_bridge.py :
detection auto /dev/cu.usbserial-*, 115200 bauds, encoding UTF-8.

Le module uart_bridge.py est garde temporairement, supprime en Task B6.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B4 : `WiFiTransport` — TCP brut ligne par ligne

**Files:**
- Modify: `webapp/transport.py` (ajout de `WiFiTransport`)
- Create: `tests/webapp/test_transport_wifi.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_transport_wifi.py`:

```python
"""Tests de WiFiTransport contre un faux serveur socket local."""
from __future__ import annotations

import socket
import threading
import time

import pytest

from webapp.transport import WiFiTransport, TransportError


@pytest.fixture
def fake_server():
    """Faux serveur TCP local. Accepte 1 client, expose les bytes reçus et permet d'en envoyer."""

    class FakeServer:
        def __init__(self):
            self.sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            self.sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            self.sock.bind(("127.0.0.1", 0))  # port aléatoire
            self.port = self.sock.getsockname()[1]
            self.sock.listen(1)
            self.client_sock = None
            self.received = bytearray()
            self.stop = False
            self.thread = threading.Thread(target=self._accept, daemon=True)
            self.thread.start()

        def _accept(self):
            try:
                self.client_sock, _ = self.sock.accept()
                self.client_sock.settimeout(0.05)
                while not self.stop:
                    try:
                        chunk = self.client_sock.recv(256)
                        if not chunk:
                            break
                        self.received.extend(chunk)
                    except socket.timeout:
                        continue
                    except OSError:
                        break
            except OSError:
                pass

        def send(self, data: bytes):
            if self.client_sock:
                self.client_sock.sendall(data)

        def shutdown(self):
            self.stop = True
            try:
                if self.client_sock:
                    self.client_sock.close()
                self.sock.close()
            except OSError:
                pass

    fs = FakeServer()
    yield fs
    fs.shutdown()


def test_wifi_transport_description_before_open():
    t = WiFiTransport(host="192.168.4.1", port=3333)
    assert "wifi" in t.description.lower()
    assert "192.168.4.1:3333" in t.description


def test_wifi_transport_open_and_write(fake_server):
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    t.write_line("PING")
    time.sleep(0.1)  # laisse le serveur recevoir
    assert fake_server.received == b"PING\n"
    t.close()


def test_wifi_transport_read_line(fake_server):
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    time.sleep(0.05)  # laisse le serveur accept()
    fake_server.send(b"PONG\n")
    assert t.read_line(timeout=1.0) == "PONG"
    t.close()


def test_wifi_transport_read_line_partial_chunks(fake_server):
    """Le buffer interne doit gérer les chunks coupés au milieu d'une ligne."""
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    time.sleep(0.05)
    fake_server.send(b"PO")
    time.sleep(0.01)
    fake_server.send(b"NG\n")
    assert t.read_line(timeout=1.0) == "PONG"
    t.close()


def test_wifi_transport_read_line_multiple_lines_one_chunk(fake_server):
    """Si deux lignes arrivent dans le même chunk, on les lit une à une."""
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    time.sleep(0.05)
    fake_server.send(b"PONG\nOK\n")
    assert t.read_line(timeout=1.0) == "PONG"
    assert t.read_line(timeout=1.0) == "OK"
    t.close()


def test_wifi_transport_read_line_timeout(fake_server):
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    assert t.read_line(timeout=0.1) is None
    t.close()


def test_wifi_transport_is_alive(fake_server):
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    assert t.is_alive is False
    t.open()
    assert t.is_alive is True
    t.close()
    assert t.is_alive is False


def test_wifi_transport_open_unreachable():
    """Ouverture sur un host injoignable doit lever TransportError dans le timeout configuré."""
    t = WiFiTransport(host="127.0.0.1", port=1, connect_timeout=0.5)
    with pytest.raises(TransportError):
        t.open()


def test_wifi_transport_write_after_close_raises(fake_server):
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    t.close()
    with pytest.raises(TransportError):
        t.write_line("PING")


def test_wifi_transport_close_idempotent(fake_server):
    t = WiFiTransport(host="127.0.0.1", port=fake_server.port)
    t.open()
    t.close()
    t.close()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_transport_wifi.py -v`
Expected: FAIL with `ImportError: cannot import name 'WiFiTransport'`

- [ ] **Step 3: Write minimal implementation**

Append to `webapp/transport.py`:

```python
import socket


class WiFiTransport(Transport):
    """Transport TCP brut vers l'ESP32 en mode AP.

    Connexion à 192.168.4.1:3333 par défaut. Protocole ligne par ligne, UTF-8.
    Buffer interne pour gérer les chunks TCP coupés au milieu d'une ligne.
    """

    def __init__(
        self,
        host: str = "192.168.4.1",
        port: int = 3333,
        connect_timeout: float = 3.0,
    ):
        self._host = host
        self._port = port
        self._connect_timeout = connect_timeout
        self._sock: socket.socket | None = None
        self._rx_buffer = bytearray()

    def open(self) -> None:
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            sock.settimeout(self._connect_timeout)
            sock.connect((self._host, self._port))
            sock.settimeout(0.1)  # lecture par défaut, ajustée par read_line
            self._sock = sock
        except (OSError, socket.timeout) as e:
            raise TransportError(
                f"connexion {self._host}:{self._port} échouée : {e}"
            ) from e

    def write_line(self, line: str) -> None:
        if self._sock is None:
            raise TransportError("WiFiTransport non ouvert")
        try:
            self._sock.sendall((line + "\n").encode("utf-8"))
        except OSError as e:
            raise TransportError(f"écriture TCP échouée : {e}") from e

    def read_line(self, timeout: float = 1.0) -> str | None:
        if self._sock is None:
            return None
        # Si une ligne complète est déjà dans le buffer, la retourner directement
        if b"\n" in self._rx_buffer:
            return self._pop_line()
        try:
            self._sock.settimeout(timeout)
            while b"\n" not in self._rx_buffer:
                chunk = self._sock.recv(256)
                if not chunk:  # connexion fermée par le pair
                    return None
                self._rx_buffer.extend(chunk)
        except socket.timeout:
            return None
        except OSError:
            return None
        return self._pop_line()

    def _pop_line(self) -> str:
        line, _, rest = self._rx_buffer.partition(b"\n")
        self._rx_buffer = bytearray(rest)
        return line.decode("utf-8", errors="replace").rstrip("\r")

    def close(self) -> None:
        if self._sock is not None:
            try:
                self._sock.shutdown(socket.SHUT_RDWR)
            except OSError:
                pass
            try:
                self._sock.close()
            except OSError:
                pass
            self._sock = None
        self._rx_buffer.clear()

    @property
    def is_alive(self) -> bool:
        return self._sock is not None

    @property
    def description(self) -> str:
        return f"wifi {self._host}:{self._port}"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_transport_wifi.py -v`
Expected: PASS (10 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/transport.py tests/webapp/test_transport_wifi.py
git commit -m "feat(transport): WiFiTransport TCP brut ligne par ligne

Connexion vers 192.168.4.1:3333 par defaut. Buffer interne pour
chunks coupes. Encodage UTF-8 explicite.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B5 : Factory `make_transport()` pilotée par env var

**Files:**
- Modify: `webapp/transport.py` (ajout de `make_transport`)
- Create: `tests/webapp/test_transport_factory.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_transport_factory.py`:

```python
"""Tests de la factory make_transport() pilotée par env var."""
from __future__ import annotations

import pytest

from webapp.transport import (
    make_transport, SerialTransport, WiFiTransport, NullTransport,
)


def test_make_transport_default_is_wifi(monkeypatch):
    monkeypatch.delenv("QUORIDOR_TRANSPORT", raising=False)
    t = make_transport()
    assert isinstance(t, WiFiTransport)


def test_make_transport_wifi_explicit(monkeypatch):
    monkeypatch.setenv("QUORIDOR_TRANSPORT", "wifi")
    t = make_transport()
    assert isinstance(t, WiFiTransport)


def test_make_transport_serial(monkeypatch):
    monkeypatch.setenv("QUORIDOR_TRANSPORT", "serial")
    t = make_transport()
    assert isinstance(t, SerialTransport)


def test_make_transport_none(monkeypatch):
    monkeypatch.setenv("QUORIDOR_TRANSPORT", "none")
    t = make_transport()
    assert isinstance(t, NullTransport)


def test_make_transport_invalid_value(monkeypatch):
    monkeypatch.setenv("QUORIDOR_TRANSPORT", "bluetooth")
    with pytest.raises(ValueError, match="QUORIDOR_TRANSPORT"):
        make_transport()


def test_make_transport_case_insensitive(monkeypatch):
    monkeypatch.setenv("QUORIDOR_TRANSPORT", "WiFi")
    t = make_transport()
    assert isinstance(t, WiFiTransport)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_transport_factory.py -v`
Expected: FAIL with `ImportError: cannot import name 'make_transport'`

- [ ] **Step 3: Write minimal implementation**

Append to `webapp/transport.py`:

```python
import os


def make_transport() -> Transport:
    """Construit le Transport selon la variable d'env QUORIDOR_TRANSPORT.

    Valeurs acceptées (case-insensitive) : 'wifi', 'serial', 'none'.
    Défaut : 'wifi'.
    """
    kind = os.environ.get("QUORIDOR_TRANSPORT", "wifi").lower()
    if kind == "wifi":
        return WiFiTransport()
    if kind == "serial":
        return SerialTransport()
    if kind == "none":
        return NullTransport()
    raise ValueError(
        f"QUORIDOR_TRANSPORT invalide : {kind!r} (attendu : wifi|serial|none)"
    )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_transport_factory.py -v`
Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/transport.py tests/webapp/test_transport_factory.py
git commit -m "feat(transport): factory make_transport() pilotee par QUORIDOR_TRANSPORT

Defaut wifi. Valeurs : wifi|serial|none. Case-insensitive.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B6 : Suppression de `uart_bridge.py` et migration des imports

**Files:**
- Delete: `webapp/uart_bridge.py`
- Delete: `tests/webapp/test_uart_bridge.py`
- Modify: `webapp/server.py` (imports + main)
- Modify: `webapp/service.py` (type hint TYPE_CHECKING)

**Note :** Cette task est un transit. Le service va encore référencer `uart_bridge` dans ses noms internes (`_uart_bridge`, etc.) jusqu'à Task C1. On gère juste la suppression du module et la mise à jour des imports.

- [ ] **Step 1: Vérifier les usages restants**

Run:
```bash
grep -rn "uart_bridge" webapp/ tests/ --include="*.py" | grep -v "test_uart_bridge.py"
```

Liste les fichiers à modifier. Doit inclure : `webapp/server.py`, `webapp/service.py`.

- [ ] **Step 2: Mettre à jour `webapp/server.py`**

Modify `webapp/server.py` lines 119-122 :

```python
def main() -> None:
    """Entrypoint CLI."""
    import uvicorn
    from webapp.transport import make_transport, TransportError

    transport = make_transport()
    startup_error: str | None = None
    try:
        transport.open()
    except TransportError as e:
        log.warning("Transport %s indisponible : %s", transport.description, e)
        startup_error = str(e)
        from webapp.transport import NullTransport
        transport = NullTransport()
        transport.open()

    app = create_app(transport=transport, startup_error=startup_error)
    app.state.service.start_tick_thread()

    log.info("Quoridor web app demarree sur http://0.0.0.0:8000 (transport=%s)",
             transport.description)
    uvicorn.run(app, host="0.0.0.0", port=8000, log_level="info")
```

Modify `webapp/server.py` `create_app` signature (lines 40-51) :

```python
def create_app(transport=None, startup_error: str | None = None) -> FastAPI:
    """Crée et configure l'application FastAPI.

    Args:
        transport: instance Transport (None pour les tests, NullTransport autrement).
        startup_error: message d'erreur si le transport demandé a échoué à l'open().
    """
    from quoridor_engine import InvalidMoveError

    app = FastAPI(title="Quoridor Demo")
    service = QuoridorService(transport=transport, startup_error=startup_error)
    app.state.service = service
    app.state.transport = transport
```

- [ ] **Step 3: Mettre à jour `webapp/service.py`**

Modify `webapp/service.py` lines 16-17 (le TYPE_CHECKING import) :

```python
if TYPE_CHECKING:
    from webapp.transport import Transport
```

Modify the `__init__` signature (line 31) :

```python
    def __init__(self, transport: Optional["Transport"] = None, startup_error: Optional[str] = None):
        # Compatibilité transitoire : on accepte transport et on adapte la propriété
        # _uart_bridge a venir. Migration complete en Task C1.
        self._uart_bridge = transport  # renommé en _plateau en Task C1
        self._startup_error = startup_error
```

**Note :** ces modifications sont **transitoires**. La task C1 fera le vrai renommage `_uart_bridge` → `_plateau`. Pour l'instant on évite de tout casser d'un coup.

- [ ] **Step 4: Supprimer les anciens fichiers**

```bash
rm webapp/uart_bridge.py
rm tests/webapp/test_uart_bridge.py
```

- [ ] **Step 5: Run tests pour vérifier qu'on n'a rien cassé**

Run: `pytest tests/ -v -m "not devkit" --ignore=tests/devkit`
Expected: tous PASS. Si un test échoue à cause d'une référence `uart_bridge`, corriger le test.

**Note attendue :** Le test `tests/webapp/test_service.py` pourrait référencer `UartBridge` ou `uart_bridge`. Si oui, mettre à jour pour utiliser un `Transport` ou un mock minimal (à examiner au cas par cas).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "refactor(webapp): supprime uart_bridge.py au profit de Transport

Le module webapp.uart_bridge est remplace par webapp.transport (3 impls).
La couche haute (PlateauBridge avec heartbeat, lock, switch) viendra en
Task C1. Pour l'instant, le service accepte un Transport directement,
sans heartbeat ni metriques (transition).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase C — Couche haute `PlateauBridge`

### Task C1 : Squelette `PlateauBridge` — wrap d'un `Transport` + lock TX

**Files:**
- Create: `webapp/plateau.py`
- Create: `tests/webapp/test_plateau_bridge.py`
- Modify: `webapp/service.py` (renommer `_uart_bridge` en `_plateau`, types)
- Modify: `webapp/server.py` (instancier `PlateauBridge` au lieu de Transport nu)

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_plateau_bridge.py`:

```python
"""Tests de PlateauBridge (couche haute au-dessus de Transport)."""
from __future__ import annotations

import threading
import time

import pytest

from webapp.transport import NullTransport, TransportError
from webapp.plateau import PlateauBridge


class FakeTransport:
    """Transport en mémoire pour tests : on contrôle ce qu'il lit, on capture ce qu'il écrit."""

    def __init__(self):
        self.written: list[str] = []
        self.to_read: list[str] = []  # queue de lignes que read_line va retourner
        self.opened = False
        self._read_lock = threading.Lock()

    def open(self) -> None:
        self.opened = True

    def write_line(self, line: str) -> None:
        if not self.opened:
            raise TransportError("not open")
        self.written.append(line)

    def read_line(self, timeout: float = 1.0) -> str | None:
        with self._read_lock:
            if self.to_read:
                return self.to_read.pop(0)
        time.sleep(min(timeout, 0.01))
        return None

    def close(self) -> None:
        self.opened = False

    @property
    def is_alive(self) -> bool:
        return self.opened

    @property
    def description(self) -> str:
        return "fake"


def test_plateau_bridge_init_with_transport():
    t = FakeTransport()
    t.open()
    b = PlateauBridge(transport=t)
    assert b.transport is t
    assert b.available is True


def test_plateau_bridge_init_with_null_transport():
    t = NullTransport()
    t.open()
    b = PlateauBridge(transport=t)
    assert b.available is False  # NullTransport.is_alive = False


def test_plateau_bridge_send_command_serializes_write_then_read():
    """Une commande envoie une ligne puis lit la réponse, dans le lock."""
    t = FakeTransport()
    t.open()
    t.to_read.append("PONG")
    b = PlateauBridge(transport=t)
    reply = b.send_command("PING", timeout=1.0)
    assert reply == "PONG"
    assert t.written == ["PING"]


def test_plateau_bridge_send_command_concurrent_serialized():
    """Deux send_command depuis 2 threads ne doivent pas s'entrelacer."""
    t = FakeTransport()
    t.open()
    # Chaque commande recevra sa propre réponse
    t.to_read.extend(["PONG", "OK"])
    b = PlateauBridge(transport=t)
    results = []

    def call(cmd):
        results.append(b.send_command(cmd, timeout=1.0))

    threads = [threading.Thread(target=call, args=(c,)) for c in ["PING", "WALL"]]
    for th in threads:
        th.start()
    for th in threads:
        th.join()
    # Les écritures ne se sont pas entrelacées : ce sont des lignes entières
    assert set(t.written) == {"PING", "WALL"}
    assert set(results) == {"PONG", "OK"}


def test_plateau_bridge_send_command_timeout_returns_none():
    t = FakeTransport()
    t.open()
    # rien dans to_read
    b = PlateauBridge(transport=t)
    reply = b.send_command("PING", timeout=0.05)
    assert reply is None


def test_plateau_bridge_close():
    t = FakeTransport()
    t.open()
    b = PlateauBridge(transport=t)
    b.close()
    assert t.opened is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_plateau_bridge.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'webapp.plateau'`

- [ ] **Step 3: Write minimal implementation**

Create file `webapp/plateau.py`:

```python
"""Couche haute au-dessus de Transport : heartbeat, lock TX, forward_move, switch.

PlateauBridge remplace l'ancien UartBridge. Elle :
- détient un Transport (Serial/WiFi/Null)
- sérialise toutes les commandes via _tx_lock (un seul write+read à la fois)
- expose les méthodes métier (forward_move avec inversion H↔V, send_home)
- maintient les compteurs heartbeat (last_pong_at, latency_avg, failed_pings)
- gère le switch_transport sans redémarrer la webapp

La gestion du heartbeat thread et de la reconnexion auto vient en Task C2 et C3.
"""
from __future__ import annotations

import logging
import threading
from typing import Optional

from webapp.transport import Transport, TransportError

log = logging.getLogger(__name__)


class PlateauBridge:
    """Couche haute pour parler à l'ESP32 via un Transport.

    Sérialise les commandes (write_line puis read_line) via un lock thread-safe.
    """

    def __init__(self, transport: Transport):
        self._transport = transport
        self._tx_lock = threading.Lock()

    @property
    def transport(self) -> Transport:
        return self._transport

    @property
    def available(self) -> bool:
        """True si le transport est ouvert et présumé fonctionnel."""
        return self._transport.is_alive

    def send_command(self, cmd: str, timeout: float = 5.0) -> Optional[str]:
        """Envoie une ligne et attend une ligne de réponse.

        Acquiert le lock TX → toute commande concurrente attend.
        Retourne None si timeout ou erreur.
        """
        with self._tx_lock:
            try:
                self._transport.write_line(cmd)
            except TransportError as e:
                log.warning("send_command(%r) : write échoué : %s", cmd, e)
                return None
            return self._transport.read_line(timeout=timeout)

    def close(self) -> None:
        """Ferme le transport sous-jacent."""
        self._transport.close()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_plateau_bridge.py -v`
Expected: PASS (6 tests)

- [ ] **Step 5: Migration `service.py` — renommage `_uart_bridge` → `_plateau`**

Modify `webapp/service.py` :

```python
# ligne 11 (imports) : ajouter
from webapp.plateau import PlateauBridge

# ligne 16-17 : TYPE_CHECKING bloc
if TYPE_CHECKING:
    pass  # plus rien à conditionnellement importer

# ligne 31-32 : __init__
    def __init__(self, transport=None, startup_error: Optional[str] = None):
        if transport is None:
            from webapp.transport import NullTransport
            transport = NullTransport()
            transport.open()
        self._plateau = PlateauBridge(transport=transport)
        self._startup_error = startup_error
```

Remplacer **toutes** les occurrences `self._uart_bridge` par `self._plateau` dans `service.py`. Vérifier :

```bash
grep -n "_uart_bridge\|uart_bridge" webapp/service.py
```

Mettre à jour les méthodes qui appellent `self._uart_bridge.send_home()` et `self._uart_bridge.forward_move()` :

```python
# Dans new_game() (ligne ~71-76) :
if plateau_mode and self._plateau.available:
    self._plateau.send_command("HOME", timeout=30.0)

# Dans _forward_to_plateau_unlocked() (lignes 336-348) :
def _forward_to_plateau_unlocked(self, move: tuple) -> None:
    """Forward best-effort au plateau physique si actif. Suppose le lock acquis."""
    if not self._plateau_mode:
        return
    if not self._plateau.available:
        return
    move_type, payload = move
    if move_type != "mur":
        return
    # Inversion H<->V (convention plateau physique)
    _SWAP = {"H": "V", "V": "H"}
    orientation = _SWAP[payload["orientation"].upper()]
    row = int(payload["row"])
    col = int(payload["col"])
    cmd = f"WALL {orientation} {row} {col}"
    reply = self._plateau.send_command(cmd, timeout=20.0)
    if reply is None or reply.startswith("ERR"):
        log.warning("WALL %s a échoué (reply=%r), désactivation plateau", cmd, reply)
        self._last_error = {
            "code": "PLATEAU_LOST",
            "message": "Plateau déconnecté, partie en mode app.",
        }
```

**Important :** ajouter un `import logging; log = logging.getLogger(__name__)` au sommet de `service.py` s'il n'existe pas déjà.

Mettre à jour `_to_dict_unlocked()` (lignes 83-92) :

```python
def _to_dict_unlocked(self) -> dict:
    plateau = {
        "available": self._plateau.available,
        "mode_active": self._plateau_mode,
        "connected": self._plateau.available and self._plateau_mode,
    }
    # ... reste identique
```

- [ ] **Step 6: Migration `server.py`**

Modify `webapp/server.py` :

```python
def create_app(transport=None, startup_error: str | None = None) -> FastAPI:
    """Crée et configure l'application FastAPI.

    Args:
        transport: instance Transport (peut être NullTransport).
        startup_error: erreur si l'open() initial a échoué.
    """
    from quoridor_engine import InvalidMoveError
    from webapp.transport import NullTransport

    if transport is None:
        transport = NullTransport()
        transport.open()

    app = FastAPI(title="Quoridor Demo")
    service = QuoridorService(transport=transport, startup_error=startup_error)
    app.state.service = service
    app.state.transport = transport
    # ... reste identique
```

Mettre à jour `post_new_game` (lignes 67-71) :

```python
    @app.post("/api/new-game")
    def post_new_game(payload: NewGamePayload):
        if payload.plateau_mode and not service._plateau.available:
            return _error_response(
                "PLATEAU_UNAVAILABLE", "Plateau non détecté.", 400
            )
```

- [ ] **Step 7: Run tests**

Run: `pytest tests/ -v -m "not devkit" --ignore=tests/devkit`
Expected: tous PASS. Si un test de `test_service.py` ou `test_api.py` échoue, mettre à jour pour passer un `Transport` au lieu de `UartBridge`.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "refactor(webapp): PlateauBridge couche haute avec lock TX

Remplace _uart_bridge par _plateau (PlateauBridge) dans le service.
PlateauBridge encapsule un Transport et serialize les commandes via
threading.Lock. Pas encore de heartbeat ni de switch (viennent C2-C5).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C2 : Heartbeat thread (PING toutes les 5 s)

**Files:**
- Modify: `webapp/plateau.py` (ajout du heartbeat)
- Modify: `tests/webapp/test_plateau_bridge.py` (tests heartbeat)

- [ ] **Step 1: Write the failing tests**

Append to `tests/webapp/test_plateau_bridge.py`:

```python
def test_plateau_bridge_starts_heartbeat_thread():
    t = FakeTransport()
    t.open()
    t.to_read.extend(["PONG"] * 100)  # toujours répond
    b = PlateauBridge(transport=t, heartbeat_interval=0.1)
    b.start_heartbeat()
    time.sleep(0.3)  # 3 cycles ≈
    b.stop_heartbeat()
    # Au moins 2 PING envoyés
    pings = [w for w in t.written if w == "PING"]
    assert len(pings) >= 2
    # last_pong_at est défini
    assert b.last_pong_at is not None
    assert b.failed_pings == 0


def test_plateau_bridge_heartbeat_detects_lost_after_2_failures():
    t = FakeTransport()
    t.open()
    # rien dans to_read → PONG manqué
    b = PlateauBridge(transport=t, heartbeat_interval=0.05, pong_timeout=0.05)
    b.start_heartbeat()
    time.sleep(0.4)  # plus de 2 cycles de 0.05+0.05
    b.stop_heartbeat()
    assert b.failed_pings >= 2
    assert b.transport_lost is True


def test_plateau_bridge_latency_avg_updated_on_pong():
    t = FakeTransport()
    t.open()
    t.to_read.extend(["PONG"] * 100)
    b = PlateauBridge(transport=t, heartbeat_interval=0.05)
    b.start_heartbeat()
    time.sleep(0.2)
    b.stop_heartbeat()
    assert b.latency_avg_ms is not None
    assert b.latency_avg_ms >= 0
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/webapp/test_plateau_bridge.py -v -k heartbeat`
Expected: FAIL with `AttributeError: 'PlateauBridge' object has no attribute 'start_heartbeat'`

- [ ] **Step 3: Étendre `PlateauBridge` avec le heartbeat**

Modify `webapp/plateau.py` (replace the class):

```python
"""Couche haute au-dessus de Transport : heartbeat, lock TX, forward_move, switch."""
from __future__ import annotations

import logging
import threading
import time
from typing import Optional

from webapp.transport import Transport, TransportError

log = logging.getLogger(__name__)


HEARTBEAT_INTERVAL_DEFAULT = 5.0
PONG_TIMEOUT_DEFAULT = 2.0
FAILED_PINGS_LIMIT = 2


class PlateauBridge:
    def __init__(
        self,
        transport: Transport,
        heartbeat_interval: float = HEARTBEAT_INTERVAL_DEFAULT,
        pong_timeout: float = PONG_TIMEOUT_DEFAULT,
    ):
        self._transport = transport
        self._tx_lock = threading.Lock()
        self._heartbeat_interval = heartbeat_interval
        self._pong_timeout = pong_timeout

        # Compteurs heartbeat (lecture par /api/status, écriture par thread heartbeat)
        self._counters_lock = threading.Lock()
        self.last_pong_at: Optional[float] = None  # time.monotonic()
        self.failed_pings: int = 0
        self.latency_avg_ms: Optional[float] = None
        self._latency_samples: list[float] = []
        self._max_samples = 10
        self.transport_lost: bool = False

        # Thread heartbeat
        self._stop_event = threading.Event()
        self._heartbeat_thread: Optional[threading.Thread] = None

    @property
    def transport(self) -> Transport:
        return self._transport

    @property
    def available(self) -> bool:
        return self._transport.is_alive and not self.transport_lost

    def send_command(self, cmd: str, timeout: float = 5.0) -> Optional[str]:
        with self._tx_lock:
            try:
                self._transport.write_line(cmd)
            except TransportError as e:
                log.warning("send_command(%r) : write échoué : %s", cmd, e)
                return None
            return self._transport.read_line(timeout=timeout)

    def start_heartbeat(self) -> None:
        if self._heartbeat_thread is not None and self._heartbeat_thread.is_alive():
            return
        self._stop_event.clear()
        self._heartbeat_thread = threading.Thread(
            target=self._heartbeat_loop, daemon=True, name="plateau-heartbeat"
        )
        self._heartbeat_thread.start()

    def stop_heartbeat(self) -> None:
        self._stop_event.set()
        if self._heartbeat_thread is not None:
            self._heartbeat_thread.join(timeout=2.0)
            self._heartbeat_thread = None

    def _heartbeat_loop(self) -> None:
        while not self._stop_event.wait(self._heartbeat_interval):
            if not self._transport.is_alive:
                continue
            t0 = time.monotonic()
            reply = self.send_command("PING", timeout=self._pong_timeout)
            if reply == "PONG":
                latency_ms = (time.monotonic() - t0) * 1000.0
                with self._counters_lock:
                    self.last_pong_at = time.monotonic()
                    self.failed_pings = 0
                    self.transport_lost = False
                    self._latency_samples.append(latency_ms)
                    if len(self._latency_samples) > self._max_samples:
                        self._latency_samples.pop(0)
                    self.latency_avg_ms = sum(self._latency_samples) / len(self._latency_samples)
            else:
                with self._counters_lock:
                    self.failed_pings += 1
                    if self.failed_pings >= FAILED_PINGS_LIMIT:
                        self.transport_lost = True
                        log.warning("Transport perdu après %d PING ratés", self.failed_pings)

    def close(self) -> None:
        self.stop_heartbeat()
        self._transport.close()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/webapp/test_plateau_bridge.py -v`
Expected: tous PASS (9 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/plateau.py tests/webapp/test_plateau_bridge.py
git commit -m "feat(plateau): heartbeat thread PING toutes les 5s

PlateauBridge demarre un thread daemon qui envoie PING toutes les 5s.
Apres 2 PONG rates consecutifs (10s par defaut), transport_lost=True.
Latence moyenne calculee sur les 10 derniers PONG.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C3 : Reconnexion auto en arrière-plan

**Files:**
- Modify: `webapp/plateau.py` (ajout du reconnect thread)
- Modify: `tests/webapp/test_plateau_bridge.py` (tests reconnect)

- [ ] **Step 1: Write the failing tests**

Append to `tests/webapp/test_plateau_bridge.py`:

```python
def test_plateau_bridge_auto_reconnect_after_lost():
    """Quand transport_lost=True, un thread reconnect tente periodiquement transport.open()."""
    t = FakeTransport()
    t.open()
    b = PlateauBridge(transport=t, heartbeat_interval=0.05, pong_timeout=0.05,
                      reconnect_interval=0.1)
    b.start_heartbeat()
    b.start_reconnect_watcher()
    time.sleep(0.4)  # cycles heartbeat → transport_lost
    assert b.transport_lost is True
    # Maintenant on simule un PONG : reconnexion réussit (transport déjà ouvert)
    t.to_read.append("PONG")
    time.sleep(0.3)  # un cycle heartbeat doit voir le PONG
    b.stop_heartbeat()
    b.stop_reconnect_watcher()
    # transport_lost devrait être revenu à False
    assert b.transport_lost is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_plateau_bridge.py -v -k auto_reconnect`
Expected: FAIL with `AttributeError: 'PlateauBridge' object has no attribute 'start_reconnect_watcher'`

- [ ] **Step 3: Étendre `PlateauBridge` avec le reconnect watcher**

Modify `webapp/plateau.py` (ajouter dans `__init__`) :

```python
        # Reconnect watcher
        self._reconnect_stop = threading.Event()
        self._reconnect_thread: Optional[threading.Thread] = None
        self._reconnect_interval = HEARTBEAT_INTERVAL_DEFAULT * 2  # par défaut 10s
```

Et accepter `reconnect_interval` dans `__init__` :

```python
    def __init__(
        self,
        transport: Transport,
        heartbeat_interval: float = HEARTBEAT_INTERVAL_DEFAULT,
        pong_timeout: float = PONG_TIMEOUT_DEFAULT,
        reconnect_interval: float = HEARTBEAT_INTERVAL_DEFAULT * 2,
    ):
        # ... code existant ...
        self._reconnect_interval = reconnect_interval
```

Ajouter les méthodes :

```python
    def start_reconnect_watcher(self) -> None:
        if self._reconnect_thread is not None and self._reconnect_thread.is_alive():
            return
        self._reconnect_stop.clear()
        self._reconnect_thread = threading.Thread(
            target=self._reconnect_loop, daemon=True, name="plateau-reconnect"
        )
        self._reconnect_thread.start()

    def stop_reconnect_watcher(self) -> None:
        self._reconnect_stop.set()
        if self._reconnect_thread is not None:
            self._reconnect_thread.join(timeout=2.0)
            self._reconnect_thread = None

    def _reconnect_loop(self) -> None:
        while not self._reconnect_stop.wait(self._reconnect_interval):
            if not self.transport_lost:
                continue
            log.info("Tentative de reconnexion %s ...", self._transport.description)
            try:
                # Fermer puis ré-ouvrir
                self._transport.close()
                self._transport.open()
                log.info("Reconnexion réussie. Le heartbeat va valider via PING.")
                # Le prochain PING du heartbeat remettra transport_lost à False si succès.
            except TransportError as e:
                log.info("Reconnexion échouée : %s", e)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/webapp/test_plateau_bridge.py -v`
Expected: tous PASS

- [ ] **Step 5: Commit**

```bash
git add webapp/plateau.py tests/webapp/test_plateau_bridge.py
git commit -m "feat(plateau): reconnect watcher arriere-plan

Thread daemon qui tente transport.open() toutes les 10s quand
transport_lost=True. Le prochain PING du heartbeat valide la reconnexion.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C4 : `switch_transport(kind)` — bascule à chaud

**Files:**
- Modify: `webapp/plateau.py` (ajout de `switch_transport`)
- Modify: `tests/webapp/test_plateau_bridge.py` (tests switch)

- [ ] **Step 1: Write the failing tests**

Append to `tests/webapp/test_plateau_bridge.py`:

```python
def test_plateau_bridge_switch_transport_success():
    t1 = FakeTransport()
    t1.open()
    b = PlateauBridge(transport=t1)
    t2 = FakeTransport()
    # switch
    b.switch_transport(t2)
    assert b.transport is t2
    assert t2.opened is True
    assert t1.opened is False  # ancien fermé


def test_plateau_bridge_switch_transport_open_failure_falls_back_to_null():
    from webapp.transport import NullTransport
    t1 = FakeTransport()
    t1.open()
    b = PlateauBridge(transport=t1)

    class FailingTransport:
        opened = False
        is_alive = False
        description = "failing"
        def open(self): raise TransportError("boom")
        def write_line(self, l): raise TransportError("boom")
        def read_line(self, timeout=1.0): return None
        def close(self): pass

    with pytest.raises(TransportError):
        b.switch_transport(FailingTransport())
    # L'ancien transport reste actif si le nouveau a échoué à open()
    assert b.transport is t1


def test_plateau_bridge_switch_resets_counters():
    t1 = FakeTransport()
    t1.open()
    b = PlateauBridge(transport=t1)
    # Simuler un état "transport_lost"
    b.transport_lost = True
    b.failed_pings = 5
    b.last_pong_at = 12345.0
    t2 = FakeTransport()
    b.switch_transport(t2)
    assert b.transport_lost is False
    assert b.failed_pings == 0
    assert b.last_pong_at is None
    assert b.latency_avg_ms is None
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/webapp/test_plateau_bridge.py -v -k switch`
Expected: FAIL with `AttributeError: 'PlateauBridge' object has no attribute 'switch_transport'`

- [ ] **Step 3: Étendre `PlateauBridge` avec `switch_transport`**

Append to `webapp/plateau.py` (méthode de la classe) :

```python
    def switch_transport(self, new_transport: Transport) -> None:
        """Bascule vers un nouveau transport en cours d'exécution.

        Acquiert _tx_lock → attend la fin d'une commande en cours.
        Ouvre le nouveau transport. Si l'open() échoue, l'ancien reste actif.
        Sinon, ferme l'ancien et bascule, puis réinitialise les compteurs.
        """
        with self._tx_lock:
            try:
                new_transport.open()
            except TransportError:
                raise  # l'ancien reste actif
            old = self._transport
            self._transport = new_transport
            try:
                old.close()
            except Exception:
                pass
            # Réinitialise les compteurs heartbeat
            with self._counters_lock:
                self.last_pong_at = None
                self.failed_pings = 0
                self.latency_avg_ms = None
                self._latency_samples = []
                self.transport_lost = False
            log.info("Bascule transport : %s", new_transport.description)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/webapp/test_plateau_bridge.py -v`
Expected: tous PASS

- [ ] **Step 5: Démarrer le heartbeat dans `server.py`**

Modify `webapp/server.py` `main()` :

```python
def main() -> None:
    """Entrypoint CLI."""
    import uvicorn
    from webapp.transport import make_transport, TransportError, NullTransport

    transport = make_transport()
    startup_error: str | None = None
    try:
        transport.open()
    except TransportError as e:
        log.warning("Transport %s indisponible : %s", transport.description, e)
        startup_error = str(e)
        transport = NullTransport()
        transport.open()

    app = create_app(transport=transport, startup_error=startup_error)
    app.state.service.start_tick_thread()
    app.state.service._plateau.start_heartbeat()
    app.state.service._plateau.start_reconnect_watcher()

    log.info("Quoridor web app demarree sur http://0.0.0.0:8000 (transport=%s)",
             transport.description)
    uvicorn.run(app, host="0.0.0.0", port=8000, log_level="info")
```

- [ ] **Step 6: Commit**

```bash
git add webapp/plateau.py webapp/server.py tests/webapp/test_plateau_bridge.py
git commit -m "feat(plateau): switch_transport bascule a chaud sous lock

Le passage WiFi <-> Serial se fait sans redemarrer la webapp. Acquiert
_tx_lock (attend fin commande en cours), ouvre le nouveau, ferme l'ancien
si succes, sinon garde l'ancien actif. Reinitialise les compteurs.

Heartbeat + reconnect demarres dans server.main().

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase D — Endpoints API + frontend

### Task D1 : Schémas Pydantic pour `/api/status` et `/api/transport/switch`

**Files:**
- Modify: `webapp/schemas.py`
- Create: `tests/webapp/test_status_schemas.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_status_schemas.py`:

```python
"""Tests des schémas Pydantic pour /api/status et /api/transport/switch."""
from __future__ import annotations

import pytest

from webapp.schemas import (
    StatusResponse, ClientStatusInfo, TransportStatusInfo, PlateauStatusInfo,
    TransportSwitchRequest, TransportSwitchResponse,
)


def test_status_response_minimal():
    s = StatusResponse(
        client=ClientStatusInfo(polling_active=True, polling_interval_ms=500),
        transport=TransportStatusInfo(
            kind="wifi",
            description="wifi 192.168.4.1:3333",
            alive=True,
            last_pong_at_iso=None,
            last_pong_age_seconds=None,
            latency_avg_ms=None,
            startup_error=None,
        ),
        plateau=PlateauStatusInfo(homed=False, ready=False),
    )
    assert s.transport.kind == "wifi"
    assert s.plateau.ready is False


def test_transport_switch_request_valid():
    req = TransportSwitchRequest(kind="serial")
    assert req.kind == "serial"


def test_transport_switch_request_invalid_kind():
    with pytest.raises(ValueError):
        TransportSwitchRequest(kind="bluetooth")  # type: ignore[arg-type]


def test_transport_switch_response_success():
    r = TransportSwitchResponse(success=True, description="serial /dev/cu.usbserial-110", error=None)
    assert r.success is True
    assert r.error is None


def test_transport_switch_response_failure():
    r = TransportSwitchResponse(success=False, description="wifi 192.168.4.1:3333", error="timeout")
    assert r.success is False
    assert r.error == "timeout"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_status_schemas.py -v`
Expected: FAIL with `ImportError`

- [ ] **Step 3: Ajouter les schémas dans `webapp/schemas.py`**

Append to `webapp/schemas.py`:

```python
TransportKind = Literal["wifi", "serial", "none"]


class ClientStatusInfo(BaseModel):
    polling_active: bool
    polling_interval_ms: int


class TransportStatusInfo(BaseModel):
    kind: TransportKind
    description: str
    alive: bool
    last_pong_at_iso: Optional[str] = None
    last_pong_age_seconds: Optional[float] = None
    latency_avg_ms: Optional[float] = None
    startup_error: Optional[str] = None


class PlateauStatusInfo(BaseModel):
    homed: bool
    ready: bool


class StatusResponse(BaseModel):
    client: ClientStatusInfo
    transport: TransportStatusInfo
    plateau: PlateauStatusInfo


class TransportSwitchRequest(BaseModel):
    kind: Literal["wifi", "serial"]  # 'none' n'est pas une cible de switch utilisateur


class TransportSwitchResponse(BaseModel):
    success: bool
    description: str
    error: Optional[str] = None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/webapp/test_status_schemas.py -v`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/schemas.py tests/webapp/test_status_schemas.py
git commit -m "feat(schemas): StatusResponse et TransportSwitchRequest/Response

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task D2 : Endpoint `GET /api/status`

**Files:**
- Modify: `webapp/plateau.py` (ajout d'une méthode `status_dict()`)
- Modify: `webapp/server.py` (ajout de la route)
- Create: `tests/webapp/test_api_status.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_api_status.py`:

```python
"""Tests de l'endpoint GET /api/status."""
from __future__ import annotations

from fastapi.testclient import TestClient

from webapp.server import create_app
from webapp.transport import NullTransport


def test_get_status_with_null_transport():
    t = NullTransport()
    t.open()
    app = create_app(transport=t, startup_error="Wi-Fi indisponible : timeout")
    client = TestClient(app)
    resp = client.get("/api/status")
    assert resp.status_code == 200
    body = resp.json()
    assert body["transport"]["kind"] == "none"
    assert body["transport"]["alive"] is False
    assert body["transport"]["startup_error"] == "Wi-Fi indisponible : timeout"
    assert body["transport"]["description"] == "none (mode autonome)"


def test_get_status_with_fake_alive_transport():
    """Avec un transport ouvert mais sans heartbeat lancé, alive=True, last_pong=None."""
    from tests.webapp.test_plateau_bridge import FakeTransport
    t = FakeTransport()
    t.open()
    app = create_app(transport=t)
    client = TestClient(app)
    resp = client.get("/api/status")
    assert resp.status_code == 200
    body = resp.json()
    # FakeTransport ne déclare pas kind, donc on accepte 'wifi' par défaut (mapping in server)
    assert body["transport"]["alive"] is True
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_api_status.py -v`
Expected: FAIL with `AssertionError` (404 ou pas la bonne structure)

- [ ] **Step 3: Ajouter `status_dict()` dans `plateau.py`**

Append to `PlateauBridge` class:

```python
    def status_dict(self) -> dict:
        """Snapshot des compteurs heartbeat, sérialisable JSON."""
        import datetime as _dt
        with self._counters_lock:
            last_pong_iso = None
            last_pong_age = None
            if self.last_pong_at is not None:
                last_pong_age = max(0.0, time.monotonic() - self.last_pong_at)
                # Approximation : on convertit monotonic en wall clock pour ISO
                last_pong_iso = (
                    _dt.datetime.now(_dt.timezone.utc) - _dt.timedelta(seconds=last_pong_age)
                ).isoformat()
            return {
                "description": self._transport.description,
                "alive": self.available,
                "last_pong_at_iso": last_pong_iso,
                "last_pong_age_seconds": last_pong_age,
                "latency_avg_ms": self.latency_avg_ms,
                "transport_lost": self.transport_lost,
                "failed_pings": self.failed_pings,
            }
```

- [ ] **Step 4: Ajouter la route dans `server.py`**

Modify `create_app` (avant la dernière ligne `return app`) :

```python
    @app.get("/api/status")
    def get_status():
        plateau_dict = service._plateau.status_dict()
        # Déduire le 'kind' de la description (préfixe)
        desc = plateau_dict["description"]
        if desc.startswith("wifi"):
            kind = "wifi"
        elif desc.startswith("serial"):
            kind = "serial"
        else:
            kind = "none"
        return {
            "client": {
                "polling_active": True,
                "polling_interval_ms": 500,
            },
            "transport": {
                "kind": kind,
                "description": desc,
                "alive": plateau_dict["alive"],
                "last_pong_at_iso": plateau_dict["last_pong_at_iso"],
                "last_pong_age_seconds": plateau_dict["last_pong_age_seconds"],
                "latency_avg_ms": plateau_dict["latency_avg_ms"],
                "startup_error": service._startup_error,
            },
            "plateau": {
                "homed": False,  # TODO Task G : suivi du homing (non bloquant pour démo)
                "ready": plateau_dict["alive"],
            },
        }
```

**Note :** le champ `plateau.homed` reste à `False` pour cette phase. Le suivi du homing physique est hors scope phase 5 (rien à valider sans plateau). Il sera mis à jour en phase 5b ou plus tard.

- [ ] **Step 5: Run tests to verify they pass**

Run: `pytest tests/webapp/test_api_status.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add webapp/plateau.py webapp/server.py tests/webapp/test_api_status.py
git commit -m "feat(api): GET /api/status renvoie l'etat transport + heartbeat

Endpoint lecture seule. Renvoie kind/description/alive/last_pong_age/
latency_avg/startup_error. Polling 2s cote frontend pour alimenter le
panneau Statut et la banniere degradee.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task D3 : Endpoint `POST /api/transport/switch`

**Files:**
- Modify: `webapp/server.py` (ajout de la route)
- Create: `tests/webapp/test_api_transport_switch.py`

- [ ] **Step 1: Write the failing test**

Create file `tests/webapp/test_api_transport_switch.py`:

```python
"""Tests de POST /api/transport/switch."""
from __future__ import annotations

from fastapi.testclient import TestClient

from webapp.server import create_app
from webapp.transport import NullTransport, TransportError


def test_transport_switch_success(monkeypatch):
    """On simule un switch vers un transport qui s'ouvre OK."""
    from tests.webapp.test_plateau_bridge import FakeTransport
    t = NullTransport()
    t.open()
    app = create_app(transport=t)

    fake_serial = FakeTransport()

    def fake_make(kind):
        if kind == "serial":
            return fake_serial
        raise ValueError(kind)

    monkeypatch.setattr("webapp.server._make_transport_by_kind", fake_make)

    client = TestClient(app)
    resp = client.post("/api/transport/switch", json={"kind": "serial"})
    assert resp.status_code == 200
    body = resp.json()
    assert body["success"] is True
    assert "fake" in body["description"]


def test_transport_switch_open_failure(monkeypatch):
    """Si le nouveau transport échoue à open(), success=False et on garde l'ancien."""
    from tests.webapp.test_plateau_bridge import FakeTransport
    t = FakeTransport()
    t.open()
    app = create_app(transport=t)

    class Failing:
        is_alive = False
        description = "wifi 192.168.4.1:3333"
        def open(self): raise TransportError("Wi-Fi injoignable")
        def write_line(self, l): pass
        def read_line(self, timeout=1.0): return None
        def close(self): pass

    monkeypatch.setattr("webapp.server._make_transport_by_kind", lambda k: Failing())

    client = TestClient(app)
    resp = client.post("/api/transport/switch", json={"kind": "wifi"})
    assert resp.status_code == 200
    body = resp.json()
    assert body["success"] is False
    assert "injoignable" in body["error"].lower()


def test_transport_switch_invalid_kind():
    t = NullTransport()
    t.open()
    app = create_app(transport=t)
    client = TestClient(app)
    resp = client.post("/api/transport/switch", json={"kind": "bluetooth"})
    assert resp.status_code == 422  # validation Pydantic
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/webapp/test_api_transport_switch.py -v`
Expected: FAIL with 404

- [ ] **Step 3: Ajouter la route + helper**

Modify `webapp/server.py` :

Add at module level (after imports) :

```python
def _make_transport_by_kind(kind: str):
    """Factory helper pour le switch endpoint, indirection facilitant les tests."""
    from webapp.transport import WiFiTransport, SerialTransport
    if kind == "wifi":
        return WiFiTransport()
    if kind == "serial":
        return SerialTransport()
    raise ValueError(f"kind invalide : {kind}")
```

Add route in `create_app` :

```python
    from webapp.schemas import TransportSwitchRequest
    from webapp.transport import TransportError

    @app.post("/api/transport/switch")
    def post_transport_switch(payload: TransportSwitchRequest):
        new_t = _make_transport_by_kind(payload.kind)
        try:
            service._plateau.switch_transport(new_t)
            service._startup_error = None
            return {
                "success": True,
                "description": service._plateau.transport.description,
                "error": None,
            }
        except TransportError as e:
            return {
                "success": False,
                "description": service._plateau.transport.description,
                "error": str(e),
            }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/webapp/test_api_transport_switch.py -v`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add webapp/server.py tests/webapp/test_api_transport_switch.py
git commit -m "feat(api): POST /api/transport/switch bascule WiFi/Serial a chaud

Acquiert _tx_lock via switch_transport(), ouvre le nouveau transport,
remet startup_error a None si succes. En cas d'echec, garde l'ancien
actif et renvoie l'erreur dans le body.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task D4 : Frontend — panneau Statut + bannière dégradée

**Files:**
- Modify: `webapp/static/index.html`
- Modify: `webapp/static/app.js`
- Modify: `webapp/static/style.css`

- [ ] **Step 1: Ajouter le markup HTML**

Modify `webapp/static/index.html` :

Juste après `<button id="btn-menu" ...>` (ligne 60), s'assurer qu'il y a une zone overlay/modal pour la liste menu. Si pas existante, ajouter au niveau racine du body (juste avant le `</body>`) :

```html
  <!-- Menu deroulant (trois points) -->
  <div id="menu-dropdown" class="menu-dropdown hidden">
    <button id="btn-menu-status" type="button">Statut Plateau</button>
  </div>

  <!-- Panneau Statut Plateau -->
  <div id="modal-status" class="modal hidden">
    <div class="modal-card">
      <h2>Statut Plateau</h2>
      <div class="status-section">
        <h3>Navigateur ↔ Mac</h3>
        <p><span id="status-client-dot" class="status-dot dot-grey"></span>
           <span id="status-client-text">—</span></p>
      </div>
      <div class="status-section">
        <h3>Mac ↔ ESP32</h3>
        <p><span id="status-transport-dot" class="status-dot dot-grey"></span>
           <span id="status-transport-text">—</span></p>
        <p class="status-detail" id="status-transport-detail">—</p>
      </div>
      <div class="status-section">
        <h3>Plateau</h3>
        <p><span id="status-plateau-dot" class="status-dot dot-grey"></span>
           <span id="status-plateau-text">—</span></p>
      </div>
      <div class="modal-actions">
        <button id="btn-status-close" type="button">Fermer</button>
      </div>
    </div>
  </div>

  <!-- Banniere degradee (visible quand transport indisponible) -->
  <div id="banner-degraded" class="banner-degraded hidden">
    <span class="banner-icon">⚠</span>
    <span class="banner-text"><strong>Plateau injoignable.</strong>
      Vérifie le branchement USB-C entre l'ESP32 et le Mac.</span>
    <button id="btn-retry-serial" type="button">Réessayer en USB</button>
    <button id="btn-retry-wifi" type="button">Réessayer en Wi-Fi</button>
  </div>
```

- [ ] **Step 2: Ajouter le polling et la logique JS**

Append to `webapp/static/app.js`:

```javascript
// ============ STATUT PLATEAU ============
let statusPollTimer = null;

async function fetchStatus() {
  try {
    const resp = await fetch("/api/status");
    if (!resp.ok) return;
    const data = await resp.json();
    renderStatus(data);
  } catch (e) {
    // erreur réseau silencieuse, on retentera
  }
}

function renderStatus(s) {
  // Indicateur Client
  const clientDot = document.getElementById("status-client-dot");
  const clientText = document.getElementById("status-client-text");
  clientDot.className = "status-dot dot-green";
  clientText.textContent = `Connecté · polling actif (${s.client.polling_interval_ms} ms)`;

  // Indicateur Transport
  const tDot = document.getElementById("status-transport-dot");
  const tText = document.getElementById("status-transport-text");
  const tDetail = document.getElementById("status-transport-detail");
  if (!s.transport.alive) {
    tDot.className = "status-dot dot-red";
    tText.textContent = "Déconnecté";
    tDetail.textContent = s.transport.startup_error
      ? `Erreur : ${s.transport.startup_error}`
      : "Le transport ne répond plus.";
  } else if (s.transport.last_pong_age_seconds !== null && s.transport.last_pong_age_seconds >= 10) {
    tDot.className = "status-dot dot-orange";
    tText.textContent = `Connecté · ${s.transport.description} (heartbeat en retard)`;
    tDetail.textContent = `Dernier PONG : il y a ${Math.round(s.transport.last_pong_age_seconds)} s`;
  } else {
    tDot.className = "status-dot dot-green";
    tText.textContent = `Connecté · ${s.transport.description}`;
    if (s.transport.last_pong_age_seconds !== null) {
      const age = Math.round(s.transport.last_pong_age_seconds);
      const lat = s.transport.latency_avg_ms !== null
        ? `${Math.round(s.transport.latency_avg_ms)} ms` : "—";
      tDetail.textContent = `Dernier PONG : il y a ${age} s · Latence moyenne : ${lat}`;
    } else {
      tDetail.textContent = "Aucun PONG reçu pour l'instant.";
    }
  }

  // Indicateur Plateau
  const pDot = document.getElementById("status-plateau-dot");
  const pText = document.getElementById("status-plateau-text");
  if (s.plateau.ready) {
    pDot.className = "status-dot dot-green";
    pText.textContent = s.plateau.homed ? "Prêt · homing effectué" : "Prêt";
  } else {
    pDot.className = "status-dot dot-grey";
    pText.textContent = "Indisponible";
  }

  // Bannière dégradée
  const banner = document.getElementById("banner-degraded");
  if (!s.transport.alive || s.transport.startup_error) {
    banner.classList.remove("hidden");
  } else {
    banner.classList.add("hidden");
  }
}

function startStatusPolling() {
  if (statusPollTimer !== null) return;
  fetchStatus();  // immédiat
  statusPollTimer = setInterval(fetchStatus, 2000);
}

document.addEventListener("DOMContentLoaded", () => {
  // Polling permanent
  startStatusPolling();

  // Menu trois points
  const btnMenu = document.getElementById("btn-menu");
  const menuDropdown = document.getElementById("menu-dropdown");
  if (btnMenu && menuDropdown) {
    btnMenu.addEventListener("click", (e) => {
      e.stopPropagation();
      menuDropdown.classList.toggle("hidden");
    });
    document.addEventListener("click", () => menuDropdown.classList.add("hidden"));
  }

  // Ouvrir panneau Statut
  const btnMenuStatus = document.getElementById("btn-menu-status");
  if (btnMenuStatus) {
    btnMenuStatus.addEventListener("click", () => {
      document.getElementById("menu-dropdown").classList.add("hidden");
      document.getElementById("modal-status").classList.remove("hidden");
    });
  }

  // Fermer panneau Statut
  const btnClose = document.getElementById("btn-status-close");
  if (btnClose) {
    btnClose.addEventListener("click", () => {
      document.getElementById("modal-status").classList.add("hidden");
    });
  }

  // Boutons bannière
  const btnRetrySerial = document.getElementById("btn-retry-serial");
  const btnRetryWifi = document.getElementById("btn-retry-wifi");
  async function switchTransport(kind) {
    try {
      const resp = await fetch("/api/transport/switch", {
        method: "POST",
        headers: {"Content-Type": "application/json"},
        body: JSON.stringify({kind}),
      });
      const data = await resp.json();
      if (!data.success) {
        alert(`Bascule ${kind} échouée : ${data.error}`);
      }
      fetchStatus();
    } catch (e) {
      alert(`Erreur réseau lors de la bascule : ${e}`);
    }
  }
  if (btnRetrySerial) btnRetrySerial.addEventListener("click", () => switchTransport("serial"));
  if (btnRetryWifi) btnRetryWifi.addEventListener("click", () => switchTransport("wifi"));
});
```

- [ ] **Step 3: Ajouter les styles CSS**

Append to `webapp/static/style.css`:

```css
/* === Panneau Statut === */
.status-section {
  margin-bottom: 16px;
}
.status-section h3 {
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #888;
  margin: 0 0 4px;
}
.status-section p {
  margin: 2px 0;
}
.status-dot {
  display: inline-block;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  margin-right: 6px;
  vertical-align: middle;
}
.dot-green { background: #2ec27e; }
.dot-orange { background: #f5a623; }
.dot-red { background: #e63946; }
.dot-grey { background: #b5b5b5; }
.status-detail {
  font-size: 12px;
  color: #666;
}

/* === Menu deroulant === */
.menu-dropdown {
  position: absolute;
  top: 50px;
  right: 16px;
  background: #fff;
  border: 1px solid #ddd;
  border-radius: 6px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  padding: 4px 0;
  z-index: 100;
}
.menu-dropdown button {
  display: block;
  width: 100%;
  padding: 8px 16px;
  border: none;
  background: transparent;
  text-align: left;
  cursor: pointer;
  font-size: 14px;
}
.menu-dropdown button:hover {
  background: #f5f5f5;
}

/* === Banniere degradee === */
.banner-degraded {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  background: #fff3cd;
  border-bottom: 2px solid #f5a623;
  padding: 10px 16px;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
  z-index: 200;
  font-size: 14px;
}
.banner-icon { font-size: 18px; }
.banner-text { flex: 1; min-width: 240px; }
.banner-degraded button {
  padding: 6px 12px;
  border: 1px solid #f5a623;
  background: #fff;
  border-radius: 4px;
  cursor: pointer;
  font-size: 13px;
}
.banner-degraded button:hover {
  background: #fff8e8;
}
```

- [ ] **Step 4: Test manuel rapide**

Run:
```bash
QUORIDOR_TRANSPORT=none python -m webapp.server &
sleep 2
curl -s http://localhost:8000/api/status | python -m json.tool
kill %1
```

Expected: JSON cohérent avec `"kind": "none"`, `"alive": false`. La webapp ouverte dans Safari doit afficher la bannière dégradée et permettre d'ouvrir le panneau Statut.

- [ ] **Step 5: Commit**

```bash
git add webapp/static/
git commit -m "feat(webapp): panneau Statut + banniere degradee + polling /api/status

- Menu trois points ouvre 'Statut Plateau' modal
- Indicateurs colores (vert/orange/rouge) selon last_pong_age_seconds
- Banniere persistante en mode degrade avec boutons 'Reessayer USB/Wifi'
- Polling toutes les 2s en permanence (alimente aussi la banniere)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase E — Firmware ESP32

### Task E1 : Extension du sketch — softAP + WiFiServer + Stream*

**Files:**
- Modify: `firmware/src/bringup_l298n_complet.cpp`

**But :** modifier le sketch existant pour ajouter le Wi-Fi sans casser l'USB-série.

**Pré-requis :** Task A1 doit avoir confirmé l'absence de `delay()` bloquants. Si présents, adapter le plan (introduire `yield()` ou découper en non-bloquant).

- [ ] **Step 1: Lire le sketch dans son intégralité (Read)**

Run: Read `firmware/src/bringup_l298n_complet.cpp` — examiner la structure de `setup()`, `loop()`, et la fonction qui dispatche les commandes (probablement nommée `handle_command` ou `processCommand` ou inline dans loop()).

- [ ] **Step 2: Identifier la fonction de dispatch des commandes**

Localiser :
- Comment le sketch lit une ligne depuis `Serial`
- Où est le `if/else` qui matche `PING`, `WALL`, `HOME`, `GOTO`, `LEVER`, `BAISSER`
- Comment il envoie la réponse (`Serial.println` ou autre)

**Décision attendue :**
- Si la fonction de dispatch est nommée et bien identifiée → refactor pour accepter un `Stream*` en argument.
- Si la logique est inline dans `loop()` → extraire dans une nouvelle fonction `void handle_command(String cmd, Stream* reply)`.

- [ ] **Step 3: Ajouter les inclusions et globales en début de fichier**

Au début de `bringup_l298n_complet.cpp`, après les `#include` existants :

```cpp
#include <WiFi.h>

const char* AP_SSID = "Quoridor-ESP32";
const char* AP_PASS = "quoridor2026";
const uint16_t TCP_PORT = 3333;
const unsigned long CLIENT_WATCHDOG_MS = 30000;

WiFiServer wifi_server(TCP_PORT);
WiFiClient wifi_client;
unsigned long last_rx_from_client = 0;
```

- [ ] **Step 4: Initialiser le Wi-Fi dans `setup()`**

Dans `setup()`, après l'init `Serial.begin(115200)` :

```cpp
  // Demarrage Wi-Fi AP
  WiFi.mode(WIFI_AP);
  bool ap_ok = WiFi.softAP(AP_SSID, AP_PASS);
  if (ap_ok) {
    Serial.print("WiFi AP demarre : ");
    Serial.print(AP_SSID);
    Serial.print(" / IP : ");
    Serial.println(WiFi.softAPIP());
  } else {
    Serial.println("WiFi AP echoue");
  }
  wifi_server.begin();
  Serial.print("Serveur TCP demarre sur port ");
  Serial.println(TCP_PORT);
```

- [ ] **Step 5: Refactor de la fonction de dispatch pour `Stream*`**

Renommer la fonction existante de dispatch en `handle_command(String cmd, Stream* reply)` et remplacer **tous** les `Serial.print*` à l'intérieur par `reply->print*`. Exemple :

```cpp
void handle_command(String cmd, Stream* reply) {
  cmd.trim();
  if (cmd == "PING") {
    reply->println("PONG");
  } else if (cmd.startsWith("WALL ")) {
    // ... logique existante de WALL, mais Serial.println -> reply->println
  } else if (cmd == "HOME") {
    // ... idem
  } else if (cmd.startsWith("GOTO ")) {
    // ... idem
  } else if (cmd == "LEVER") {
    // ... idem
  } else if (cmd == "BAISSER") {
    // ... idem
  } else {
    reply->print("ERR commande inconnue : ");
    reply->println(cmd);
  }
}
```

- [ ] **Step 6: Étendre `loop()` pour le canal Wi-Fi**

Dans `loop()`, remplacer la lecture série existante par :

```cpp
void loop() {
  // 1. Accepter une nouvelle connexion TCP (politique "dernier client gagne")
  if (wifi_server.hasClient()) {
    if (wifi_client && wifi_client.connected()) {
      wifi_client.stop();
    }
    wifi_client = wifi_server.available();
    last_rx_from_client = millis();
    Serial.print("[WiFi] Nouveau client : ");
    Serial.println(wifi_client.remoteIP());
  }

  // 2. Lire une ligne depuis USB-serie (canal existant)
  if (Serial.available()) {
    String cmd = Serial.readStringUntil('\n');
    handle_command(cmd, &Serial);
  }

  // 3. Lire une ligne depuis le client TCP
  if (wifi_client && wifi_client.connected() && wifi_client.available()) {
    String cmd = wifi_client.readStringUntil('\n');
    last_rx_from_client = millis();
    handle_command(cmd, &wifi_client);
  }

  // 4. Watchdog : drop client TCP si 30s sans trafic
  if (wifi_client && wifi_client.connected()
      && (millis() - last_rx_from_client > CLIENT_WATCHDOG_MS)) {
    Serial.println("[WiFi] Watchdog : client silencieux, drop");
    wifi_client.stop();
  }

  // 5. Reste de la loop existante (ne pas dupliquer le bloc lecture serie)
  // ... le reste du code original (gestion CoreXY, etc.) reste tel quel ...
}
```

**Important :** ne **pas** garder l'ancienne lecture série en doublon. La nouvelle lecture (point 2) la remplace.

- [ ] **Step 7: Build firmware**

Run:
```bash
pio run -e <env_name>  # remplacer <env_name> par celui défini dans platformio.ini
```

Expected: build SUCCESS. Si erreur de compilation, lire le message et corriger (typiquement : `Stream*` n'est pas reconnu sans `#include <Stream.h>`, mais en général Arduino le tire automatiquement).

- [ ] **Step 8: Flash + monitor série**

Run:
```bash
pio run --target upload -e <env_name>
pio device monitor --port /dev/cu.usbserial-110 --baud 115200
```

Expected (au boot ESP32) :
```
WiFi AP demarre : Quoridor-ESP32 / IP : 192.168.4.1
Serveur TCP demarre sur port 3333
```

Puis tester un `PING` au moniteur série → doit répondre `PONG`.

- [ ] **Step 9: Commit**

```bash
git add firmware/src/bringup_l298n_complet.cpp
git commit -m "feat(firmware): ajout transport Wi-Fi AP + WiFiServer port 3333

- WiFi.softAP(Quoridor-ESP32, quoridor2026) au boot
- WiFiServer sur port 3333, politique 'dernier client gagne'
- Watchdog 30s pour dropper les clients TCP silencieux
- handle_command(cmd, Stream* reply) refactor avec polymorphisme Stream
- USB-serie reste actif en parallele (canal existant inchange)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase F — Outil `wifi_switch` + tests devkit

### Task F1 : Outil `tools/wifi_switch.py` (CLI macOS networksetup)

**Files:**
- Create: `tools/__init__.py` (vide)
- Create: `tools/wifi_switch.py`

- [ ] **Step 1: Créer le fichier vide `__init__.py`**

Create file `tools/__init__.py` (contenu vide).

- [ ] **Step 2: Créer `tools/wifi_switch.py`**

Create file `tools/wifi_switch.py`:

```python
"""Helper macOS pour basculer le Wi-Fi du Mac entre 'Quoridor-ESP32' et le SSID maison.

Usage :
    python tools/wifi_switch.py to-esp32    # bascule sur Quoridor-ESP32
    python tools/wifi_switch.py restore     # restaure le SSID précédent
    python tools/wifi_switch.py status      # affiche le SSID courant

Sauvegarde le SSID précédent dans /tmp/quoridor_previous_ssid.
Détecte dynamiquement le nom de l'interface Wi-Fi via networksetup.
"""
from __future__ import annotations

import argparse
import re
import subprocess
import sys
from pathlib import Path

PREVIOUS_SSID_FILE = Path("/tmp/quoridor_previous_ssid")
ESP32_SSID = "Quoridor-ESP32"
ESP32_PASS = "quoridor2026"


def find_wifi_interface() -> str:
    """Détecte le nom de l'interface Wi-Fi macOS (typiquement 'en0')."""
    out = subprocess.check_output(["networksetup", "-listallhardwareports"], text=True)
    blocks = re.split(r"\n(?=Hardware Port:)", out)
    for block in blocks:
        if "Wi-Fi" in block:
            m = re.search(r"Device:\s*(\S+)", block)
            if m:
                return m.group(1)
    raise RuntimeError("Interface Wi-Fi non détectée via networksetup")


def get_current_ssid(iface: str) -> str | None:
    """Retourne le SSID actuel ou None si déconnecté."""
    out = subprocess.check_output(
        ["networksetup", "-getairportnetwork", iface], text=True
    )
    m = re.search(r":\s*(.+)$", out.strip())
    if m:
        ssid = m.group(1).strip()
        if "not associated" in ssid.lower():
            return None
        return ssid
    return None


def set_ssid(iface: str, ssid: str, password: str | None = None) -> None:
    cmd = ["networksetup", "-setairportnetwork", iface, ssid]
    if password:
        cmd.append(password)
    subprocess.check_call(cmd)


def cmd_to_esp32() -> int:
    iface = find_wifi_interface()
    current = get_current_ssid(iface)
    if current and current != ESP32_SSID:
        PREVIOUS_SSID_FILE.write_text(current)
        print(f"SSID precedent sauvegarde : {current}")
    set_ssid(iface, ESP32_SSID, ESP32_PASS)
    print(f"Bascule sur {ESP32_SSID} via {iface}")
    return 0


def cmd_restore() -> int:
    iface = find_wifi_interface()
    if not PREVIOUS_SSID_FILE.exists():
        print(f"Aucun SSID precedent sauvegarde dans {PREVIOUS_SSID_FILE}")
        return 1
    previous = PREVIOUS_SSID_FILE.read_text().strip()
    set_ssid(iface, previous)
    print(f"Restaure SSID precedent : {previous}")
    return 0


def cmd_status() -> int:
    iface = find_wifi_interface()
    current = get_current_ssid(iface)
    print(f"Interface : {iface}")
    print(f"SSID actuel : {current if current else '(non connecte)'}")
    if PREVIOUS_SSID_FILE.exists():
        print(f"SSID precedent en cache : {PREVIOUS_SSID_FILE.read_text().strip()}")
    return 0


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["to-esp32", "restore", "status"])
    args = parser.parse_args()
    if args.action == "to-esp32":
        return cmd_to_esp32()
    elif args.action == "restore":
        return cmd_restore()
    elif args.action == "status":
        return cmd_status()
    return 2


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3: Test manuel**

Run:
```bash
python tools/wifi_switch.py status
```

Expected: affiche le SSID Wi-Fi actuel du Mac et l'interface (probablement `en0`).

**Ne pas tester `to-esp32` à cette étape** — il faut que le firmware soit déjà flashé pour que l'AP existe. Test complet en Task F4.

- [ ] **Step 4: Commit**

```bash
git add tools/
git commit -m "feat(tools): wifi_switch.py CLI macOS networksetup

3 commandes : to-esp32 / restore / status. Detecte l'interface Wi-Fi
dynamiquement (pas hardcode en0). Sauvegarde le SSID precedent dans
/tmp/quoridor_previous_ssid.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task F2 : Markers pytest devkit + fixture `wifi_fixture`

**Files:**
- Modify: `pyproject.toml`
- Create: `tests/devkit/__init__.py`
- Create: `tests/devkit/conftest.py`

- [ ] **Step 1: Ajouter les markers `devkit_serial` et `devkit_wifi`**

Modify `pyproject.toml` section `[tool.pytest.ini_options]` :

```toml
[tool.pytest.ini_options]
markers = [
  "devkit: tests qui requièrent le DevKit ESP32 branché (skipés sinon)",
  "devkit_serial: tests devkit nécessitant l'ESP32 en USB-série",
  "devkit_wifi: tests devkit nécessitant l'ESP32 en mode AP Wi-Fi",
]
testpaths = ["tests"]
```

- [ ] **Step 2: Créer le package tests/devkit**

Create file `tests/devkit/__init__.py` (contenu vide).

- [ ] **Step 3: Créer la fixture `wifi_fixture`**

Create file `tests/devkit/conftest.py`:

```python
"""Fixtures pour tests devkit (ESP32 branché)."""
from __future__ import annotations

import subprocess
import time
from pathlib import Path

import pytest


@pytest.fixture
def wifi_fixture():
    """Bascule le Mac sur Quoridor-ESP32 pendant le test, restaure après.

    Suppose que `tools/wifi_switch.py` est dispo et que l'ESP32 est en AP.
    """
    project_root = Path(__file__).resolve().parents[2]
    switch = project_root / "tools" / "wifi_switch.py"

    # Bascule
    subprocess.check_call(["python", str(switch), "to-esp32"])
    # Laisse 3s à la liaison Wi-Fi
    time.sleep(3.0)

    yield

    # Restaure
    try:
        subprocess.check_call(["python", str(switch), "restore"])
        time.sleep(2.0)
    except subprocess.CalledProcessError:
        pass  # ne pas masquer une erreur de test par une erreur de restauration
```

- [ ] **Step 4: Vérifier que pytest découvre bien les markers**

Run: `pytest --markers | grep devkit`
Expected: 3 lignes (`devkit`, `devkit_serial`, `devkit_wifi`).

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml tests/devkit/
git commit -m "feat(tests): markers devkit_serial/devkit_wifi + wifi_fixture

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task F3 : Test devkit USB — handshake `PING`/`PONG`

**Files:**
- Create: `tests/devkit/test_handshake_serial.py`

- [ ] **Step 1: Créer le test**

Create file `tests/devkit/test_handshake_serial.py`:

```python
"""Test devkit USB : PING/PONG via USB-série avec ESP32 branché."""
from __future__ import annotations

import pytest

from webapp.transport import SerialTransport


@pytest.mark.devkit_serial
def test_ping_pong_serial():
    """Envoie PING via USB, attend PONG."""
    t = SerialTransport()  # détection auto /dev/cu.usbserial-*
    t.open()
    try:
        t.write_line("PING")
        reply = t.read_line(timeout=3.0)
        assert reply == "PONG", f"Attendu 'PONG', reçu {reply!r}"
    finally:
        t.close()


@pytest.mark.devkit_serial
def test_unknown_command_returns_err_serial():
    """Une commande inconnue doit renvoyer une ligne commençant par ERR."""
    t = SerialTransport()
    t.open()
    try:
        t.write_line("FOOBAR")
        reply = t.read_line(timeout=3.0)
        assert reply is not None
        assert reply.startswith("ERR"), f"Attendu 'ERR ...', reçu {reply!r}"
    finally:
        t.close()
```

- [ ] **Step 2: Run le test (ESP32 doit être branché)**

Run: `pytest tests/devkit/test_handshake_serial.py -v -m devkit_serial`
Expected: 2 PASS.

Si échec : vérifier que l'ESP32 est branché et que `ls /dev/cu.usbserial-*` retourne au moins un port.

- [ ] **Step 3: Commit**

```bash
git add tests/devkit/test_handshake_serial.py
git commit -m "test(devkit): handshake PING/PONG + ERR via USB-serie

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task F4 : Test devkit Wi-Fi — handshake `PING`/`PONG`

**Files:**
- Create: `tests/devkit/test_handshake_wifi.py`

- [ ] **Step 1: Créer le test**

Create file `tests/devkit/test_handshake_wifi.py`:

```python
"""Test devkit Wi-Fi : PING/PONG via TCP avec ESP32 en mode AP."""
from __future__ import annotations

import pytest

from webapp.transport import WiFiTransport


@pytest.mark.devkit_wifi
def test_ping_pong_wifi(wifi_fixture):
    """Envoie PING via TCP 192.168.4.1:3333, attend PONG."""
    t = WiFiTransport()
    t.open()
    try:
        t.write_line("PING")
        reply = t.read_line(timeout=3.0)
        assert reply == "PONG", f"Attendu 'PONG', reçu {reply!r}"
    finally:
        t.close()
```

- [ ] **Step 2: Run le test**

**Pré-requis** : ESP32 flashé avec firmware Task E1, AP `Quoridor-ESP32` actif, Mac toujours connecté à un Wi-Fi.

Run: `pytest tests/devkit/test_handshake_wifi.py -v -m devkit_wifi`
Expected: PASS.

Le déroulé attendu :
1. `wifi_fixture` bascule le Mac sur `Quoridor-ESP32`
2. Sleep 3s
3. Le test ouvre TCP `192.168.4.1:3333`, envoie PING, lit PONG
4. `wifi_fixture` restaure le Wi-Fi maison

**Si la première bascule demande confirmation GUI macOS** : valider manuellement la première fois. Les fois suivantes seront silencieuses.

- [ ] **Step 3: Commit**

```bash
git add tests/devkit/test_handshake_wifi.py
git commit -m "test(devkit): handshake PING/PONG via Wi-Fi + wifi_fixture

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task F5 : Test devkit "politique dernier client gagne" (Wi-Fi)

**Files:**
- Create: `tests/devkit/test_last_client_wins.py`

- [ ] **Step 1: Créer le test**

Create file `tests/devkit/test_last_client_wins.py`:

```python
"""Test devkit : la politique 'dernier client gagne' côté firmware."""
from __future__ import annotations

import time

import pytest

from webapp.transport import WiFiTransport


@pytest.mark.devkit_wifi
def test_last_client_wins(wifi_fixture):
    """Quand un 2e client se connecte, l'ESP32 doit dropper le 1er."""
    c1 = WiFiTransport()
    c1.open()
    c1.write_line("PING")
    assert c1.read_line(timeout=2.0) == "PONG"

    c2 = WiFiTransport()
    c2.open()
    time.sleep(0.3)  # laisse l'ESP32 traiter la nouvelle connexion
    c2.write_line("PING")
    assert c2.read_line(timeout=2.0) == "PONG"

    # c1 doit avoir été déconnecté côté ESP32
    # On envoie un PING : si l'ESP32 a stoppé c1, soit l'écriture échoue,
    # soit la lecture timeout (pas de PONG)
    try:
        c1.write_line("PING")
    except Exception:
        pass  # peut lever si socket fermé
    assert c1.read_line(timeout=1.0) is None, "c1 ne devrait plus recevoir de PONG"

    c2.close()
```

- [ ] **Step 2: Run le test**

Run: `pytest tests/devkit/test_last_client_wins.py -v -m devkit_wifi`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/devkit/test_last_client_wins.py
git commit -m "test(devkit): politique 'dernier client gagne' cote firmware

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task F6 : Test devkit "coexistence USB + Wi-Fi simultanée"

**Files:**
- Create: `tests/devkit/test_serial_and_wifi_coexist.py`

- [ ] **Step 1: Créer le test**

Create file `tests/devkit/test_serial_and_wifi_coexist.py`:

```python
"""Test devkit : USB-série et Wi-Fi simultanément actifs côté firmware (régression)."""
from __future__ import annotations

import pytest

from webapp.transport import SerialTransport, WiFiTransport


@pytest.mark.devkit
def test_serial_and_wifi_coexist(wifi_fixture):
    """Les deux canaux répondent indépendamment."""
    s = SerialTransport()
    s.open()
    w = WiFiTransport()
    w.open()
    try:
        # Alternance rapide pour vérifier qu'aucun canal ne mange l'autre
        for _ in range(5):
            s.write_line("PING")
            assert s.read_line(timeout=2.0) == "PONG"
            w.write_line("PING")
            assert w.read_line(timeout=2.0) == "PONG"
    finally:
        s.close()
        w.close()
```

- [ ] **Step 2: Run le test**

Run: `pytest tests/devkit/test_serial_and_wifi_coexist.py -v -m devkit`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/devkit/test_serial_and_wifi_coexist.py
git commit -m "test(devkit): coexistence USB + Wi-Fi simultanee (regression)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task F7 : Test devkit "WALL parser" (sans plateau)

**Files:**
- Create: `tests/devkit/test_wall_parser.py`

- [ ] **Step 1: Créer le test**

Create file `tests/devkit/test_wall_parser.py`:

```python
"""Test devkit : le parser WALL répond ERR sur une position invalide (sans plateau)."""
from __future__ import annotations

import pytest

from webapp.transport import SerialTransport


@pytest.mark.devkit_serial
def test_wall_parser_invalid_orientation_returns_err():
    """WALL avec une orientation hors {H,V} doit renvoyer ERR."""
    t = SerialTransport()
    t.open()
    try:
        t.write_line("WALL Z 0 0")
        reply = t.read_line(timeout=3.0)
        assert reply is not None
        assert reply.startswith("ERR"), f"Attendu ERR, reçu {reply!r}"
    finally:
        t.close()


@pytest.mark.devkit_serial
def test_wall_parser_out_of_bounds_returns_err():
    """WALL hors grille 6x6 doit renvoyer ERR."""
    t = SerialTransport()
    t.open()
    try:
        t.write_line("WALL H 99 99")
        reply = t.read_line(timeout=3.0)
        assert reply is not None
        assert reply.startswith("ERR"), f"Attendu ERR, reçu {reply!r}"
    finally:
        t.close()
```

**Note** : ces tests vérifient le **parser** uniquement (commandes invalides). Un test `WALL H 0 0` valide n'est **pas** ajouté ici car il déclencherait un mouvement moteur qui, sans plateau, peut bloquer le sketch. À ajouter en Niveau 3 (intégration plateau).

- [ ] **Step 2: Run les tests**

Run: `pytest tests/devkit/test_wall_parser.py -v -m devkit_serial`
Expected: 2 PASS.

**Risque** : si le firmware ne valide pas les paramètres avant d'agir, il peut bloquer attendant un capteur. Si le test timeout sans réponse, il faut **ajuster le firmware** pour valider strictement les arguments avant d'entamer le mouvement. À noter dans le commit si correction firmware requise.

- [ ] **Step 3: Commit**

```bash
git add tests/devkit/test_wall_parser.py
git commit -m "test(devkit): parser WALL renvoie ERR sur arguments invalides

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase G — Validation finale

### Task G1 : Run complet `pytest -m "not devkit"`

- [ ] **Step 1: Lancer la suite complète sans hardware**

Run: `pytest -m "not devkit" -v`
Expected: tous les tests passent (Niveau 1 complet).

- [ ] **Step 2: Vérifier la couverture**

Run: `pytest -m "not devkit" --cov=webapp --cov=quoridor_engine --cov-report=term-missing`
Expected: couverture > 80% sur `webapp/transport.py` et `webapp/plateau.py`.

- [ ] **Step 3: Pas de commit** — c'est de la validation.

---

### Task G2 : Run devkit USB

- [ ] **Step 1: Brancher l'ESP32 en USB**

Vérifier : `ls /dev/cu.usbserial-*` retourne au moins un port.

- [ ] **Step 2: Run les tests USB**

Run: `pytest -m devkit_serial -v`
Expected: tous PASS.

- [ ] **Step 3: Pas de commit.**

---

### Task G3 : Run devkit Wi-Fi (avec bascule réseau)

- [ ] **Step 1: Vérifier l'AP**

Run: `python tools/wifi_switch.py status` (depuis le Mac)
Vérifier que l'AP `Quoridor-ESP32` est dans la liste des Wi-Fi détectés (via le menu macOS si besoin).

- [ ] **Step 2: Run les tests Wi-Fi**

Run: `pytest -m devkit_wifi -v`
Expected: tous PASS. Le Mac se bascule automatiquement entre `Quoridor-ESP32` et le Wi-Fi maison.

**Si premier passage GUI** : valider manuellement la connexion à `Quoridor-ESP32` quand macOS demande, une seule fois.

- [ ] **Step 3: Pas de commit.**

---

### Task G4 : Validation manuelle webapp Wi-Fi

- [ ] **Step 1: Lancer la webapp en mode Wi-Fi**

Run:
```bash
python tools/wifi_switch.py to-esp32   # Mac sur AP ESP32
QUORIDOR_TRANSPORT=wifi python -m webapp.server &
sleep 3
```

Expected: log de démarrage indique `transport=wifi 192.168.4.1:3333`. Pas d'erreur.

- [ ] **Step 2: Ouvrir Safari sur `http://localhost:8000`**

Vérifier :
- Pas de bannière dégradée
- Cliquer sur `⋯` (menu) → option "Statut Plateau" apparaît
- Cliquer dessus → panneau s'ouvre
- Section "Mac ↔ ESP32" affiche `● Connecté · WiFi · 192.168.4.1:3333`
- "Dernier PONG : il y a < 10 s" se met à jour toutes les 2 s
- Latence affichée (typiquement 5-20 ms)

- [ ] **Step 3: Tester le mode dégradé manuellement**

Débrancher l'USB-C de l'ESP32 (ou couper l'alim, peu importe — couper l'AP).

Attendre 15 s (2 × heartbeat × pong_timeout). Vérifier dans la webapp :
- La bannière apparaît : "⚠ Plateau injoignable..."
- Le panneau Statut affiche `● Déconnecté` en rouge
- Cliquer sur "Réessayer en Wi-Fi" → si l'AP est revenu : OK, sinon "Wi-Fi injoignable"

- [ ] **Step 4: Restaurer le Wi-Fi maison**

Run:
```bash
kill %1   # stop webapp
python tools/wifi_switch.py restore
```

- [ ] **Step 5: Commit du log de validation (optionnel)**

Si tu veux laisser une trace, créer `docs/superpowers/plans/2026-05-20-phase5-validation.md` avec :

```markdown
# Validation phase 5 — 2026-05-20

- [x] pytest -m "not devkit" : tous OK
- [x] pytest -m devkit_serial : tous OK
- [x] pytest -m devkit_wifi : tous OK
- [x] webapp en mode wifi : panneau Statut affiche bien WiFi + PONG < 10s
- [x] mode dégradé : bannière apparaît, bouton "Réessayer" fonctionne
```

Puis :
```bash
git add docs/superpowers/plans/2026-05-20-phase5-validation.md
git commit -m "docs: log validation phase 5 (Wi-Fi + USB + degrade)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Récap des commits attendus (séquentiel)

```
B1: feat(transport): interface abstraite Transport + TransportError
B2: feat(transport): NullTransport pour mode autonome explicite
B3: feat(transport): SerialTransport (migration de uart_bridge)
B4: feat(transport): WiFiTransport TCP brut ligne par ligne
B5: feat(transport): factory make_transport() pilotee par QUORIDOR_TRANSPORT
B6: refactor(webapp): supprime uart_bridge.py au profit de Transport
C1: refactor(webapp): PlateauBridge couche haute avec lock TX
C2: feat(plateau): heartbeat thread PING toutes les 5s
C3: feat(plateau): reconnect watcher arriere-plan
C4: feat(plateau): switch_transport bascule a chaud sous lock
D1: feat(schemas): StatusResponse et TransportSwitchRequest/Response
D2: feat(api): GET /api/status renvoie l'etat transport + heartbeat
D3: feat(api): POST /api/transport/switch bascule WiFi/Serial a chaud
D4: feat(webapp): panneau Statut + banniere degradee + polling /api/status
E1: feat(firmware): ajout transport Wi-Fi AP + WiFiServer port 3333
F1: feat(tools): wifi_switch.py CLI macOS networksetup
F2: feat(tests): markers devkit_serial/devkit_wifi + wifi_fixture
F3: test(devkit): handshake PING/PONG + ERR via USB-serie
F4: test(devkit): handshake PING/PONG via Wi-Fi + wifi_fixture
F5: test(devkit): politique 'dernier client gagne' cote firmware
F6: test(devkit): coexistence USB + Wi-Fi simultanee (regression)
F7: test(devkit): parser WALL renvoie ERR sur arguments invalides
G4: docs: log validation phase 5 (optionnel)
```

23 commits, scope cohérent à chaque commit.
